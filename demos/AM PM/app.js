;(()=>{const CART_KEY="menumaker.catalog.cart.v2";const app=document.getElementById("app");document.documentElement.setAttribute("data-runtime-boot","loading");const carouselInstances=new Map();const state={catalog:null,runtimeManifest:null,locale:"en",page:"home",navOpen:false,filters:new Set(),allergenFilters:new Set(),selectedProductId:"",selectedProductSectionId:"",detailOpen:false,cartOpen:false,cart:readCart()};let lastScrollY=0;let headerHidden=false;let headerScrollTicking=false;const runtimeAssetPathIndex=new Map();const runtimeAssetIndexByKey=new Map();const runtimeCacheBlobUrls=new Map();const runtimeCacheIndexByBucket=new Map();const cacheBucketSizes={list:24*1024*1024,detail:64*1024*1024};const posterPlaceholderSrc="data:image/gif;base64,R0lGODlhAQABAPAAAAAAAAAAACH5BAEAAAAALAAAAAABAAEAAAICRAEAOw==";const runtimeEntryUrls=window.__MENUMAKER_RUNTIME_ENTRY_URLS__&&typeof window.__MENUMAKER_RUNTIME_ENTRY_URLS__==="object"?window.__MENUMAKER_RUNTIME_ENTRY_URLS__:{};const optionalVendorLoads=new Map();const visibleCarouselSectionIds=new Set();const sprite360FrameMetaCache=new Map();const sprite360VisibleFrameIndexCache=new Map();const listSpriteControllerByCanvas=new WeakMap();let focusedCarouselSectionId="";let focusedCarouselSectionPinnedUntil=0;let focusedCarouselSectionPinnedScrollY=0;let carouselFocusUpdateQueued=false;let hasUserInteraction=false;let activeListSpriteController=null;let detailReturnScrollY=0;let pendingScrollRestoreY=null;const carouselSelectedIndexBySection=new Map();const detailMotionCueSeenKeys=new Set();let syncingFaqAccordion=false;const copyByLocale={en:{a:"Add to cart",b:"Address",c:"Avoid allergens",d:"About us",e:"Cart",f:"Close",g:"Clear cart",h:"Choose at least {count} options for {group}.",i:"Choose at most {count} options for {group}.",j:"Choose one option for {group}.",k:"Complete the required selections before adding this item.",l:"Customer",m:"Decrease quantity",n:"Enter the delivery address.",o:"Enter the table number.",p:"Fee",q:"Frequently asked questions",r:"From",s:"Fulfillment",t:"Get directions",u:"Contact us",v:"Home",w:"Hours",x:"Increase quantity",y:"item",z:"items",A:"Loading menu...",B:"Menu",C:"Map preview unavailable",D:"Mode",E:"Name",F:"No products added yet.",G:"No dishes match your filters.",H:"Note",I:"Next",J:"Order",K:"Order note",L:"Previous",M:"Product note",N:"Open navigation",O:"Phone",P:"Quantity",Q:"Required",R:"Sections",S:"Select",T:"Send WhatsApp order",U:"360 unavailable",V:"Subtotal",W:"Table",X:"Total",Y:"Unavailable",Z:"Unable to load catalog.",aa:"Highlights",ab:"360°",ac:"No",ad:"External apps; separate cart."},es:{a:"Agregar al carrito",b:"Direccion",c:"Evitar alergenos",d:"¿Quiénes somos?",e:"Carrito",f:"Cerrar",g:"Vaciar carrito",h:"Elige al menos {count} opciones para {group}.",i:"Elige maximo {count} opciones para {group}.",j:"Elige una opcion para {group}.",k:"Completa las selecciones requeridas antes de agregar este item.",l:"Cliente",m:"Disminuir cantidad",n:"Ingresa la direccion de entrega.",o:"Ingresa el numero de mesa.",p:"Cargo",q:"Preguntas frecuentes",r:"Desde",s:"Modo de pedido",t:"Como llegar",u:"Contacto",v:"Inicio",w:"Horario",x:"Aumentar cantidad",y:"item",z:"items",A:"Cargando menu...",B:"Menu",C:"Mapa no disponible",D:"Modo",E:"Nombre",F:"Todavia no hay productos.",G:"No hay platillos con esos filtros.",H:"Nota",I:"Siguiente",J:"Pedido",K:"Nota general",L:"Anterior",M:"Nota del producto",N:"Abrir navegacion",O:"Telefono",P:"Cantidad",Q:"Requerido",R:"Secciones",S:"Seleccionar",T:"Enviar pedido por WhatsApp",U:"360 no disponible",V:"Subtotal",W:"Mesa",X:"Total",Y:"No disponible",Z:"No se pudo cargar el catalogo.",aa:"Etiquetas",ab:"360°",ac:"Sin",ad:"Apps externas; carrito separado."}};const copy=(key)=>{const localeCopy=copyByLocale[state.locale]||copyByLocale.en;return localeCopy[key]||copyByLocale.en[key]||key;};const formatCopy=(key,replacements)=>{let text=copy(key);Object.entries(replacements||{}).forEach(([name,value])=>{text=text.split("{"+name+"}").join(String(value));});return text;};const escapeHtml=(value)=>String(value??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g, "&quot;");

  const renderMarkdown = (value) => {
    const html = escapeHtml(value)
      .replace(/^###\s+(.+)$/gm, "<h5>$1</h5>")
      .replace(/^##\s+(.+)$/gm, "<h4>$1</h4>")
      .replace(/^#\s+(.+)$/gm, "<h3>$1</h3>")
      .replace(/^[-*]\s+(.+)$/gm, "<p>•$1</p>")
      .replace(/\*\*([^*]+)\*\*/g, "<strong>$1</strong>")
      .replace(/\*([^*]+)\*/g, "<em>$1</em>")
      .replace(/\n{2,}/g, "</p><p>")
      .replace(/\n/g, "<br>");
    return /<h[3-5]|•/.test(html) ? html : "<p>" + html + "</p>";
  };

  const catalogIconPaths = {"cart":"<circle cx=\"8\" cy=\"21\" r=\"1\"></circle><circle cx=\"19\" cy=\"21\" r=\"1\"></circle><path d=\"M2.05 2.05h2l2.66 12.42a2 2 0 0 0 2 1.58h9.78a2 2 0 0 0 1.95-1.57l1.65-7.43H5.12\"></path>","chevronLeft":"<path d=\"m15 18-6-6 6-6\"></path>","chevronRight":"<path d=\"m9 18 6-6-6-6\"></path>","close":"<path d=\"M18 6 6 18\"></path><path d=\"m6 6 12 12\"></path>","flame":"<path d=\"M12 3q1 4 4 6.5t3 5.5a1 1 0 0 1-14 0 5 5 0 0 1 1-3 1 1 0 0 0 5 0c0-2-1.5-3-1.5-5q0-2 2.5-4\"></path>","home":"<path d=\"M15 21v-8a1 1 0 0 0-1-1h-4a1 1 0 0 0-1 1v8\"></path><path d=\"M3 10a2 2 0 0 1 .709-1.528l7-6a2 2 0 0 1 2.582 0l7 6A2 2 0 0 1 21 10v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z\"></path>","info":"<circle cx=\"12\" cy=\"12\" r=\"10\"></circle><path d=\"M12 16v-4\"></path><path d=\"M12 8h.01\"></path>","mail":"<path d=\"m22 7-8.991 5.727a2 2 0 0 1-2.009 0L2 7\"></path><rect x=\"2\" y=\"4\" width=\"20\" height=\"16\" rx=\"2\"></rect>","mapPin":"<path d=\"M20 10c0 4.993-5.539 10.193-7.399 11.799a1 1 0 0 1-1.202 0C9.539 20.193 4 14.993 4 10a8 8 0 0 1 16 0\"></path><circle cx=\"12\" cy=\"10\" r=\"3\"></circle>","menu":"<path d=\"M4 5h16\"></path><path d=\"M4 12h16\"></path><path d=\"M4 19h16\"></path>","foodMenu":"<path d=\"M3 2v7c0 1.1.9 2 2 2h4a2 2 0 0 0 2-2V2\"></path><path d=\"M7 2v20\"></path><path d=\"M21 15V2a5 5 0 0 0-5 5v6c0 1.1.9 2 2 2h3Zm0 0v7\"></path>","minus":"<path d=\"M5 12h14\"></path>","plus":"<path d=\"M5 12h14\"></path><path d=\"M12 5v14\"></path>","trash":"<path d=\"M10 11v6\"></path><path d=\"M14 11v6\"></path><path d=\"M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6\"></path><path d=\"M3 6h18\"></path><path d=\"M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2\"></path>","whatsapp":"<path d=\"M2.992 16.342a2 2 0 0 1 .094 1.167l-1.065 3.29a1 1 0 0 0 1.236 1.168l3.413-.998a2 2 0 0 1 1.099.092 10 10 0 1 0-4.777-4.719\"></path>","chefHat":"<path d=\"M17 21a1 1 0 0 0 1-1v-5.35c0-.457.316-.844.727-1.041a4 4 0 0 0-2.134-7.589 5 5 0 0 0-9.186 0 4 4 0 0 0-2.134 7.588c.411.198.727.585.727 1.041V20a1 1 0 0 0 1 1Z\"></path><path d=\"M6 17h12\"></path>","iceCream":"<path d=\"M12 17c5 0 8-2.69 8-6H4c0 3.31 3 6 8 6m-4 4h8m-4-3v3M5.14 11a3.5 3.5 0 1 1 6.71 0\"></path><path d=\"M12.14 11a3.5 3.5 0 1 1 6.71 0\"></path><path d=\"M15.5 6.5a3.5 3.5 0 1 0-7 0\"></path>","wine":"<path d=\"M8 22h8\"></path><path d=\"M7 10h10\"></path><path d=\"M12 15v7\"></path><path d=\"M12 15a5 5 0 0 0 5-5c0-2-.5-4-2-8H9c-1.5 4-2 6-2 8a5 5 0 0 0 5 5Z\"></path>"};function icon(name){const path=catalogIconPaths[name];if(!path)return"";return'<svg class="catalog-icon catalog-icon--'+escapeHtml(name)+'" viewBox="0 0 24 24" aria-hidden="true" focusable="false">'+path+'</svg>';}const buttonContent=(iconName,label)=>'<span class="catalog-button-content">'+icon(iconName)+'<span>'+escapeHtml(label)+'</span></span>';const safeHttpsUrl=(value)=>{const raw=String(value||"").trim();if(!raw)return"";try{const url=new URL(raw);return url.protocol==="https:"?url.href:"";}catch{return"";}};const runtimeAssetKey=(role,sectionId,itemId)=>[String(role||"").trim(),String(sectionId||"").trim(),String(itemId||"").trim()].join("::");const normalizeAssetUrl=(value)=>{const path=String(value||"").trim();if(/^(blob:|data:|https?:\/\/|\/)/.test(path))return path;return"./"+path.replace(/^\.\//,"");};const runtimeEntryUrl=(entryName)=>{const mapped=runtimeEntryUrls&&typeof runtimeEntryUrls[entryName]==="string"?runtimeEntryUrls[entryName]:"";return mapped||("./"+String(entryName||"").replace(/^\.\//,""));};const ensureOptionalVendor=(globalName,entryName)=>{if(window[globalName])return Promise.resolve(true);if(optionalVendorLoads.has(globalName))return optionalVendorLoads.get(globalName);const promise=new Promise((resolve)=>{const script=document.createElement("script");script.src=runtimeEntryUrl(entryName);script.async=true;script.onload=()=>resolve(Boolean(window[globalName]));script.onerror=()=>resolve(false);document.head.appendChild(script);});optionalVendorLoads.set(globalName,promise);return promise;};const readConnectionPolicy=()=>{const connection=navigator.connection||navigator.mozConnection||navigator.webkitConnection;const effectiveType=String(connection?.effectiveType||"").trim().toLowerCase();const saveData=Boolean(connection?.saveData);const coarsePointer=window.matchMedia("(pointer: coarse)").matches;if(saveData){return{profile:"save-data",listNeighborRadius:0,allowListSprite360:false,allowIntentPrefetch:false,allowBackgroundWarmup:false};}if(effectiveType==="slow-2g"||effectiveType==="2g"){return{profile:"2g",listNeighborRadius:0,allowListSprite360:false,allowIntentPrefetch:false,allowBackgroundWarmup:false};}if(effectiveType==="3g"){return{profile:"3g",listNeighborRadius:0,allowListSprite360:true,allowIntentPrefetch:false,allowBackgroundWarmup:false};}if((!coarsePointer&&window.innerWidth>=900)||window.innerWidth>=1100){return{profile:"desktop",listNeighborRadius:2,allowListSprite360:true,allowIntentPrefetch:true,allowBackgroundWarmup:true};}return{profile:"4g",listNeighborRadius:1,allowListSprite360:true,allowIntentPrefetch:coarsePointer?false:true,allowBackgroundWarmup:coarsePointer?false:true};};function registerRuntimeManifest(manifest){runtimeAssetPathIndex.clear();runtimeAssetIndexByKey.clear();if(!manifest||!Array.isArray(manifest.assets)){state.runtimeManifest=null;return;}state.runtimeManifest=manifest;manifest.assets.forEach((asset)=>{const outputPath=String(asset.outputPath||"").trim();if(outputPath){runtimeAssetPathIndex.set(outputPath,asset);}const key=runtimeAssetKey(asset.role,asset.sectionId,asset.itemId);if(!runtimeAssetIndexByKey.has(key)){runtimeAssetIndexByKey.set(key,asset);}});}const runtimeAssetFor=(role,sectionId,itemId,fallbackPath)=>{const direct=runtimeAssetIndexByKey.get(runtimeAssetKey(role,sectionId,itemId));if(direct)return direct;const byPath=fallbackPath?runtimeAssetPathIndex.get(String(fallbackPath).trim()):null;return byPath||null;};const assetSource=(value,sectionId,itemId,role)=>{const path=String(value||"").trim();const runtimeAsset=runtimeAssetFor(role,sectionId,itemId,path);return normalizeAssetUrl(runtimeAsset?.runtimePath||path);};const localStorageSafe=()=>{try{return window.localStorage;}catch{return null;}};const cacheBucketName=(bucket)=>"menumaker.catalog2."+bucket+"."+String(state.runtimeManifest?.exportVersion||"v1");const cacheIndexStorageKey=(bucket)=>"menumaker.catalog2.cache-index."+bucket+"."+String(state.runtimeManifest?.exportVersion||"v1");const readCacheIndex=(bucket)=>{if(runtimeCacheIndexByBucket.has(bucket))return runtimeCacheIndexByBucket.get(bucket);const storage=localStorageSafe();if(!storage){const empty={};runtimeCacheIndexByBucket.set(bucket,empty);return empty;}try{const parsed=JSON.parse(storage.getItem(cacheIndexStorageKey(bucket))||"{}");const next=parsed&&typeof parsed==="object"?parsed:{};runtimeCacheIndexByBucket.set(bucket,next);return next;}catch{const empty={};runtimeCacheIndexByBucket.set(bucket,empty);return empty;}};const writeCacheIndex=(bucket,index)=>{runtimeCacheIndexByBucket.set(bucket,index);const storage=localStorageSafe();if(!storage)return;try{storage.setItem(cacheIndexStorageKey(bucket),JSON.stringify(index));}catch{return;}};const touchCacheIndexEntry=(bucket,src,bytes)=>{const index={...readCacheIndex(bucket)};index[src]={bytes:Math.max(0,Number(bytes||0)),updatedAt:Date.now()};writeCacheIndex(bucket,index);return index;};const trimCacheBucket=async(bucket)=>{if(!("caches"in window))return;const limit=cacheBucketSizes[bucket]||cacheBucketSizes.detail;const index={...readCacheIndex(bucket)};const entries=Object.entries(index);let totalBytes=entries.reduce((sum,[,entry])=>sum+Math.max(0,Number(entry?.bytes||0)),0);if(totalBytes<=limit)return;const cache=await caches.open(cacheBucketName(bucket));const staleEntries=entries.sort((left,right)=>Number(left[1]?.updatedAt||0)-Number(right[1]?.updatedAt||0));for(const[src,entry]of staleEntries){if(totalBytes<=limit)break;totalBytes-=Math.max(0,Number(entry?.bytes||0));delete index[src];try{await cache.delete(src);}catch{continue;}}writeCacheIndex(bucket,index);};const resolveCachedBlobUrl=async(src,bucket,bytes,cacheOnSuccess)=>{if(!src)return src;const cachedBlobUrl=runtimeCacheBlobUrls.get(src);if(cachedBlobUrl){touchCacheIndexEntry(bucket,src,bytes);return cachedBlobUrl;}if(!("caches"in window)||!window.isSecureContext)return src;const cache=await caches.open(cacheBucketName(bucket));let response=await cache.match(src);if(!response){response=await fetch(src,{cache:"force-cache"}).catch(()=>null);if(response&&response.ok&&cacheOnSuccess!==false){await cache.put(src,response.clone()).catch(()=>undefined);}}if(!response||!response.ok)return src;const blob=await response.clone().blob().catch(()=>null);if(!blob)return src;const blobUrl=URL.createObjectURL(blob);runtimeCacheBlobUrls.set(src,blobUrl);touchCacheIndexEntry(bucket,src,bytes!=null?bytes:blob.size);await trimCacheBucket(bucket);return blobUrl;};const warmAssetInCache=async(src,bucket,bytes)=>{if(!src||!("caches"in window)||!window.isSecureContext)return;const cache=await caches.open(cacheBucketName(bucket));const cached=await cache.match(src);if(cached){touchCacheIndexEntry(bucket,src,bytes);return;}const response=await fetch(src,{cache:"force-cache"}).catch(()=>null);if(!response||!response.ok)return;await cache.put(src,response.clone()).catch(()=>undefined);touchCacheIndexEntry(bucket,src,bytes);await trimCacheBucket(bucket);};const sprite360Cache=new Map();const sprite360ReadyCacheKeys=new Set();function sprite360CacheKey(src,bucket){return String(bucket||"detail")+"::"+String(src||"");}function isSprite360ImageReady(src,bucket){return Boolean(src)&&sprite360ReadyCacheKeys.has(sprite360CacheKey(src,bucket));}function loadSprite360Image(src,options={}){if(!src)return Promise.reject(new Error("Missing sprite360 source"));const bucket=options.bucket||"detail";const cacheKey=sprite360CacheKey(src,bucket);const cached=sprite360Cache.get(cacheKey);if(cached)return cached;const task=resolveCachedBlobUrl(src,bucket,options.bytes,options.cacheOnSuccess).then((resolvedSrc)=>new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>reject(new Error("sprite360 failed to load"));image.src=resolvedSrc;})).then((image)=>{sprite360ReadyCacheKeys.add(cacheKey);return image;}).catch((error)=>{sprite360Cache.delete(cacheKey);sprite360ReadyCacheKeys.delete(cacheKey);throw error;});sprite360Cache.set(cacheKey,task);return task;}function resolveInteractiveOrbitFrame(rect){const left=Number(rect.left)||0;const top=Number(rect.top)||0;const width=Math.max(0,Number(rect.width)||0);const height=Math.max(0,Number(rect.height)||0);const rx=width/2;const ry=height/2;return{left,top,width,height,cx:left+rx,cy:top+ry,rx,ry,arcRadiusPx:(rx+ry)/2,deadZoneRadiusPx:Math.min(width,height)*0.14};}function readInteractiveOrbitSample(clientX,clientY,frame){if(frame.width<=0||frame.height<=0||clientX<frame.left||clientX>frame.left+frame.width||clientY<frame.top||clientY>frame.top+frame.height){return{kind:"outside-host"};}const dx=clientX-frame.cx;const dy=clientY-frame.cy;const distancePx=Math.hypot(dx,dy);if(distancePx<=frame.deadZoneRadiusPx){return{kind:"dead-zone"};}const normalizedDx=frame.rx>0?dx/frame.rx:0;const normalizedDy=frame.ry>0?dy/frame.ry:0;return{kind:"sample",angleRad:Math.atan2(normalizedDy,normalizedDx)};}function unwrapInteractiveAngleDelta(prevAngleRad,nextAngleRad){let delta=nextAngleRad-prevAngleRad;while(delta<=-Math.PI)delta+=Math.PI*2;while(delta>Math.PI)delta-=Math.PI*2;return delta;}function resolveInteractiveVirtualDeltaPx(prevAngleRad,nextAngleRad,frame){return-unwrapInteractiveAngleDelta(prevAngleRad,nextAngleRad)*frame.arcRadiusPx;}function resolveInteractivePixelsPerFrame(frame,frameCount){return Math.max(1,(Math.PI*(frame.rx+frame.ry))/Math.max(2,frameCount));}function renderSpriteFrame(context,image,frame,frameIndex){const normalized=((Math.round(frameIndex)%frame.frameCount)+frame.frameCount)%frame.frameCount;const column=normalized%frame.columns;const row=Math.floor(normalized/frame.columns);context.clearRect(0,0,frame.frameWidth,frame.frameHeight);context.drawImage(image,column*frame.frameWidth,row*frame.frameHeight,frame.frameWidth,frame.frameHeight,0,0,frame.frameWidth,frame.frameHeight);}function resolveSpriteFrameFromImage(image,frame){const naturalWidth=Math.max(1,Math.round(Number(image.naturalWidth||image.width||0)));const naturalHeight=Math.max(1,Math.round(Number(image.naturalHeight||image.height||0)));let frameWidth=Math.max(1,Math.round(Number(frame.frameWidth||0)));let frameHeight=Math.max(1,Math.round(Number(frame.frameHeight||0)));let columns=Math.max(1,Math.round(Number(frame.columns||0)));let rows=Math.max(1,Math.round(Number(frame.rows||0)));let frameCount=Math.max(2,Math.round(Number(frame.frameCount||0)));const fps=Math.max(1,Number(frame.fps||12));if(columns*frameWidth>naturalWidth&&columns>1){frameWidth=Math.max(1,Math.floor(naturalWidth/columns));}if(rows*frameHeight>naturalHeight&&rows>1){frameHeight=Math.max(1,Math.floor(naturalHeight/rows));}const inferredColumns=Math.max(1,Math.floor(naturalWidth/frameWidth));const inferredRows=Math.max(1,Math.floor(naturalHeight/frameHeight));const declaredTiles=columns*rows;const inferredTiles=inferredColumns*inferredRows;const looksLikeDefaultMetadata=frameCount<=2&&declaredTiles<=2&&inferredTiles>declaredTiles;const imageHasLargerGrid=inferredTiles>declaredTiles&&(naturalWidth>=frameWidth*(columns+1)||naturalHeight>=frameHeight*(rows+1));if(looksLikeDefaultMetadata||imageHasLargerGrid){columns=inferredColumns;rows=inferredRows;frameCount=inferredTiles;}else{frameCount=Math.min(frameCount,declaredTiles);}return{frameCount,columns,rows,frameWidth,frameHeight,fps};}function canvasHasVisiblePixels(context,width,height){const safeWidth=Math.max(1,Math.floor(width||0));const safeHeight=Math.max(1,Math.floor(height||0));try{const samplesPerAxis=7;for(let yIndex=0;yIndex<samplesPerAxis;yIndex+=1){for(let xIndex=0;xIndex<samplesPerAxis;xIndex+=1){const x=Math.min(safeWidth-1,Math.max(0,Math.floor((safeWidth*(xIndex+0.5))/samplesPerAxis)));const y=Math.min(safeHeight-1,Math.max(0,Math.floor((safeHeight*(yIndex+0.5))/samplesPerAxis)));if(context.getImageData(x,y,1,1).data[3]>8)return true;}}return false;}catch{return true;}}function canvasHasVisiblePixelsPrecise(context,width,height){const safeWidth=Math.max(1,Math.floor(width||0));const safeHeight=Math.max(1,Math.floor(height||0));try{const alpha=context.getImageData(0,0,safeWidth,safeHeight).data;for(let index=3;index<alpha.length;index+=4){if(alpha[index]>8)return true;}return false;}catch{return true;}}function resolveVisibleSpriteFrameIndexes(context,image,frame){const indexes=[];for(let frameIndex=0;frameIndex<frame.frameCount;frameIndex+=1){renderSpriteFrame(context,image,frame,frameIndex);if(canvasHasVisiblePixels(context,frame.frameWidth,frame.frameHeight)){indexes.push(frameIndex);}}context.clearRect(0,0,frame.frameWidth,frame.frameHeight);return indexes;}function resolveSpriteFrameIndexes(frameCount){return Array.from({length:Math.max(0,Math.round(frameCount||0))},(_,index)=>index);}function isSpriteFrameVisible(context,image,frame,frameIndex,precise){renderSpriteFrame(context,image,frame,frameIndex);return precise?canvasHasVisiblePixelsPrecise(context,frame.frameWidth,frame.frameHeight):canvasHasVisiblePixels(context,frame.frameWidth,frame.frameHeight);}function spritePlaybackCacheKey(src,frameMeta){return[src,frameMeta.frameCount,frameMeta.columns,frameMeta.rows,frameMeta.frameWidth,frameMeta.frameHeight,frameMeta.fps].join("|");}function resolveSpritePlaybackFrames(src,image,frameMeta,context){const cacheKey=spritePlaybackCacheKey(src,frameMeta);let frame=sprite360FrameMetaCache.get(cacheKey);if(!frame){frame=resolveSpriteFrameFromImage(image,frameMeta);sprite360FrameMetaCache.set(cacheKey,frame);}let visibleFrameIndexes=sprite360VisibleFrameIndexCache.get(cacheKey);if(!visibleFrameIndexes){const sequentialIndexes=resolveSpriteFrameIndexes(frame.frameCount);if(!sequentialIndexes.length){visibleFrameIndexes=[];}else if(!isSpriteFrameVisible(context,image,frame,sequentialIndexes[0],true)){visibleFrameIndexes=resolveVisibleSpriteFrameIndexes(context,image,frame);}else{visibleFrameIndexes=sequentialIndexes;}context.clearRect(0,0,frame.frameWidth,frame.frameHeight);sprite360VisibleFrameIndexCache.set(cacheKey,visibleFrameIndexes);}return{frame,visibleFrameIndexes};}function renderVisibleSpriteFrame(context,image,frame,visibleIndexes,cursor){if(!visibleIndexes.length)return false;const normalized=((Math.round(cursor)%visibleIndexes.length)+visibleIndexes.length)%visibleIndexes.length;renderSpriteFrame(context,image,frame,visibleIndexes[normalized]);return true;}function resolveVisibleSpriteFrameIndex(visibleIndexes,cursor){if(!visibleIndexes.length)return-1;const normalized=((Math.round(cursor)%visibleIndexes.length)+visibleIndexes.length)%visibleIndexes.length;return Number(visibleIndexes[normalized]??-1);}const textOf=(value,locale,fallback)=>(value&&(value[locale]||value[fallback]||Object.values(value)[0]))||"";const localText=(value)=>textOf(value,state.locale,state.catalog.meta.defaultLocale);const pageIds=["home","menu","about","contact"];function pageFromHash(){const raw=decodeURIComponent(String(window.location.hash||"").replace(/^#/,"")).trim().toLowerCase();const aliases={inicio:"home",home:"home",menu:"menu","menú":"menu",ubicacion:"contact",ubicaciones:"contact",location:"contact",locations:"contact","quienes-somos":"about","quiénes-somos":"about",about:"about",contacto:"contact",contact:"contact"};const page=aliases[raw]||raw;return pageIds.includes(page)?page:"home";}function pageHash(page){switch(page){case"menu":return"menu";case"about":return"quienes-somos";case"contact":return"contacto";default:return"inicio";}}function pageLabel(page){switch(page){case"menu":return copy("B");case"about":return copy("d");case"contact":return copy("u");default:return copy("v");}}function setPage(page,pushHistory=true){const nextPage=pageIds.includes(page)?page:"home";state.page=nextPage;state.navOpen=false;pendingScrollRestoreY=null;if(nextPage!=="menu")state.cartOpen=false;render();if(pushHistory&&window.history?.pushState){try{window.history.pushState(null,"","#"+pageHash(nextPage));}catch{}}window.scrollTo({top:0,behavior:window.matchMedia("(prefers-reduced-motion: reduce)").matches?"auto":"smooth"});}const money=(amount)=>{const catalog=state.catalog;const settings=catalog.meta.money;const rounded=Math.round((Number(amount||0)+Number.EPSILON)*100)/100;const value=Number.isInteger(rounded)?String(rounded):rounded.toFixed(2);const display=settings.decimalSeparator==="comma"?value.replace(".",","):value;return settings.currencyPosition==="left"?settings.currency+display:display+settings.currency;};const deltaMoney=(amount)=>{const value=Number(amount||0);if(!value)return"";return(value>0?"+":"-")+money(Math.abs(value));};function readCart(){try{const parsed=JSON.parse(sessionStorage.getItem(CART_KEY)||"{}");return{items:Array.isArray(parsed.items)?parsed.items:[],fulfillmentModeId:parsed.fulfillmentModeId||"",customer:parsed.customer||{},note:parsed.note||""};}catch{return{items:[],fulfillmentModeId:"",customer:{},note:""};}}function saveCart(){sessionStorage.setItem(CART_KEY,JSON.stringify(state.cart));}function clearCart(){state.cart={...state.cart,items:[]};sessionStorage.removeItem(CART_KEY);}const productById=(id)=>state.catalog.products.find((product)=>product.id===id);const groupById=(id)=>state.catalog.modifierGroups.find((group)=>group.id===id);const modifierRefKey=(ref)=>ref?.refId||ref?.groupId||"";function applyGroupRef(group,ref){const overrides=new Map((ref.optionOverrides||[]).map((entry)=>[entry.optionId,entry]));return{...group,id:modifierRefKey(ref),label:ref.label||group.label,required:ref.required??group.required,minSelected:ref.minSelected??group.minSelected,maxSelected:ref.maxSelected??group.maxSelected,options:group.options.map((option)=>{const override=overrides.get(option.id);return override?{...option,...override,id:option.id}:option;})};}function productPriceCandidates(product){const base=Number(product.basePrice||0);const candidates=[base];const add=(value)=>{const rounded=Math.round((Number(value||0)+Number.EPSILON)*100)/100;if(Number.isFinite(rounded)&&rounded>=0)candidates.push(rounded);};(product.priceRules||[]).filter((rule)=>rule.enabled!==false).filter((rule)=>Number.isFinite(rule.priceOverride)&&rule.priceOverride>=0).forEach((rule)=>add(rule.priceOverride));(product.modifierGroupRefs||[]).forEach((ref)=>{const group=groupById(ref.groupId);if(!group)return;const resolved=applyGroupRef(group,ref);const minSelected=Math.max(resolved.required?1:0,Number(resolved.minSelected||0));const maxSelected=Math.max(minSelected,Number(resolved.maxSelected||0));if(minSelected<1||maxSelected!==1)return;resolved.options.filter((option)=>option.available).forEach((option)=>{if(typeof option.priceOverride==="number"&&Number.isFinite(option.priceOverride)){add(option.priceOverride);return;}if(Number(option.priceDelta||0))add(base+Number(option.priceDelta||0));});});return Array.from(new Set(candidates)).sort((left,right)=>left-right);}function productPriceLabel(product,context){const candidates=productPriceCandidates(product);const min=candidates[0]??Number(product.basePrice||0);const max=candidates[candidates.length-1]??min;if(max-min>=0.01){return copy("r")+" "+money(min);}if(context==="detail"){return copy("r")+" "+money(min);}return money(min);}const defaultOptionIds=(group)=>group.options.filter((option)=>option.defaultSelected).map((option)=>option.id);const defaultChoiceIds=(slot)=>slot.choices.filter((choice)=>choice.defaultSelected).map((choice)=>choice.id);function selectedOptionIds(selection,defaults){return selection&&Array.isArray(selection.optionIds)?selection.optionIds:defaults;}function selectedChoiceIds(selection,defaults){return selection&&Array.isArray(selection.choiceIds)?selection.choiceIds:defaults;}function setSelectedOptions(selectedOptionsByGroup,ref,optionIds){const validIds=optionIds.filter(Boolean);if(!validIds.length)return;const refKey=modifierRefKey(ref);if(refKey)selectedOptionsByGroup.set(refKey,new Set(validIds));const grouped=selectedOptionsByGroup.get(ref.groupId)||new Set();validIds.forEach((id)=>grouped.add(id));selectedOptionsByGroup.set(ref.groupId,grouped);}function selectedOptionsForCondition(condition,selectedOptionsByGroup){return selectedOptionsByGroup.get(condition.refId||condition.groupId);}function groupRefApplies(ref,selectedOptionsByGroup){const dependency=ref.dependsOn;if(!dependency||!dependency.groupId||!Array.isArray(dependency.optionIds)||!dependency.optionIds.length)return true;const selected=selectedOptionsForCondition(dependency,selectedOptionsByGroup);return Boolean(selected&&dependency.optionIds.some((optionId)=>selected.has(optionId)));}function defaultProductPriceRule(product){return(product.priceRules||[]).find((rule)=>rule.defaultSelected&&rule.enabled!==false&&Number.isFinite(rule.priceOverride)&&Array.isArray(rule.modifierConditions))||null;}function defaultOptionIdsForGroup(product,group,ref){const rule=defaultProductPriceRule(product);const refKey=modifierRefKey(ref);const condition=(rule?.modifierConditions||[]).find((entry)=>entry.groupId===ref.groupId&&(!entry.refId||entry.refId===refKey));return condition&&Array.isArray(condition.optionIds)&&condition.optionIds.length?condition.optionIds:defaultOptionIds(group);}function priceRuleScore(rule){return(rule.modifierConditions||[]).reduce((score,condition)=>score+Math.max(1,(condition.optionIds||[]).length),0);}function matchingProductPriceRule(product,selectedOptionsByGroup){const matches=(product.priceRules||[]).map((rule,index)=>({rule,index})).filter((entry)=>entry.rule.enabled!==false).filter((entry)=>Number.isFinite(entry.rule.priceOverride)&&entry.rule.priceOverride>=0).filter((entry)=>Array.isArray(entry.rule.modifierConditions)&&entry.rule.modifierConditions.length).filter((entry)=>entry.rule.modifierConditions.every((condition)=>{const selected=selectedOptionsForCondition(condition,selectedOptionsByGroup);return Boolean(selected&&Array.isArray(condition.optionIds)&&condition.optionIds.length&&condition.optionIds.every((optionId)=>selected.has(optionId)));})).sort((left,right)=>priceRuleScore(right.rule)-priceRuleScore(left.rule)||left.index-right.index);const rule=matches[0]?.rule;return rule?{id:rule.id,priceOverride:Number(rule.priceOverride||0),displayGroupId:rule.displayPriceGroupId||rule.modifierConditions?.[0]?.groupId||""}:null;}function priceRuleCoversModifier(priceRule,modifier,product){if(!priceRule)return false;const sourceRule=(product.priceRules||[]).find((entry)=>entry.id===priceRule.id);return Boolean(sourceRule&&(sourceRule.modifierConditions||[]).some((condition)=>condition.groupId===modifier.groupId&&(!condition.refId||condition.refId===modifier.refId)&&Array.isArray(condition.optionIds)&&condition.optionIds.includes(modifier.optionId)));}function minimumSelectionMessage(label,count){return count<=1?formatCopy("j",{group:label}):formatCopy("h",{count,group:label});}function maximumSelectionMessage(label,count){return formatCopy("i",{count,group:label});}function validateProductSelection(product,selection){const issues=[];const modifierSelections=new Map((selection.modifierSelections||[]).map((entry)=>[entry.refId||entry.groupId,entry]));const comboSelections=new Map((selection.comboSelections||[]).map((entry)=>[entry.slotId,entry]));const quantity=Math.max(0,Number(selection.quantity||0));if(!quantity){issues.push({kind:"quantity",id:"quantity",message:copy("P")});}if(!product.available){issues.push({kind:"product",id:product.id,message:copy("Y")});}const selectedOptionsByGroup=new Map();(product.modifierGroupRefs||[]).forEach((ref)=>{const group=groupById(ref.groupId);if(!group)return;if(!groupRefApplies(ref,selectedOptionsByGroup))return;const resolved=applyGroupRef(group,ref);const label=localText(resolved.label);const minSelected=Math.max(resolved.required?1:0,Number(resolved.minSelected||0));const maxSelected=Math.max(minSelected,Number(resolved.maxSelected||0));const refKey=modifierRefKey(ref);const ids=selectedOptionIds(modifierSelections.get(refKey),defaultOptionIdsForGroup(product,resolved,ref));const validIds=ids.filter((id)=>{const option=resolved.options.find((entry)=>entry.id===id);return option&&option.available;});setSelectedOptions(selectedOptionsByGroup,ref,validIds);if(validIds.length<minSelected){issues.push({kind:"modifier",id:refKey,message:minimumSelectionMessage(label,minSelected)});}if(ids.length>maxSelected){issues.push({kind:"modifier",id:refKey,message:maximumSelectionMessage(label,maxSelected)});}});(product.comboSlots||[]).forEach((slot)=>{const label=localText(slot.label);const minSelected=Math.max(slot.required?1:0,Number(slot.minSelected||0));const maxSelected=Math.max(minSelected,Number(slot.maxSelected||0));const ids=selectedChoiceIds(comboSelections.get(slot.id),defaultChoiceIds(slot));const validIds=ids.filter((id)=>{const choice=slot.choices.find((entry)=>entry.id===id);return choice&&choice.available;});if(validIds.length<minSelected){issues.push({kind:"combo",id:slot.id,message:minimumSelectionMessage(label,minSelected)});}if(ids.length>maxSelected){issues.push({kind:"combo",id:slot.id,message:maximumSelectionMessage(label,maxSelected)});}});return issues;}function defaultProductSelection(product){const selectedOptionsByGroup=new Map();const modifierSelections=[];(product.modifierGroupRefs||[]).forEach((ref)=>{const group=groupById(ref.groupId);const resolved=group?applyGroupRef(group,ref):null;if(!resolved||!groupRefApplies(ref,selectedOptionsByGroup))return;const optionIds=defaultOptionIdsForGroup(product,resolved,ref).filter((id)=>resolved.options.some((option)=>option.id===id&&option.available));setSelectedOptions(selectedOptionsByGroup,ref,optionIds);modifierSelections.push({groupId:ref.groupId,...(ref.refId?{refId:ref.refId}:{}),optionIds});});return{productId:product.id,quantity:1,modifierSelections,comboSelections:(product.comboSlots||[]).map((slot)=>({slotId:slot.id,choiceIds:defaultChoiceIds(slot)})),note:""};}function lineKey(selection){return JSON.stringify({productId:selection.productId,modifierSelections:selection.modifierSelections||[],comboSelections:selection.comboSelections||[],note:selection.note||""});}function addCartSelection(selection){const normalized={...selection,quantity:Math.max(1,Number(selection.quantity||1))};const key=lineKey(normalized);const index=state.cart.items.findIndex((item)=>lineKey(item)===key);if(index>=0){state.cart.items[index]={...state.cart.items[index],quantity:state.cart.items[index].quantity+normalized.quantity};}else{state.cart.items.push(normalized);}saveCart();}function priceLine(selection){const product=productById(selection.productId);if(!product)return null;const modifierSelections=new Map((selection.modifierSelections||[]).map((entry)=>[entry.refId||entry.groupId,entry]));const comboSelections=new Map((selection.comboSelections||[]).map((entry)=>[entry.slotId,entry]));const modifiers=[];const comboChoices=[];const selectedOptionsByGroup=new Map();let modifierTotal=0;let comboTotal=0;let basePriceOverride=null;(product.modifierGroupRefs||[]).forEach((ref)=>{const group=groupById(ref.groupId);if(!group)return;if(!groupRefApplies(ref,selectedOptionsByGroup))return;const resolved=applyGroupRef(group,ref);const refKey=modifierRefKey(ref);const ids=selectedOptionIds(modifierSelections.get(refKey),defaultOptionIdsForGroup(product,resolved,ref));ids.forEach((id)=>{const option=resolved.options.find((entry)=>entry.id===id);if(!option)return;const hasPriceOverride=typeof option.priceOverride==="number"&&Number.isFinite(option.priceOverride);if(hasPriceOverride){basePriceOverride=Number(option.priceOverride||0);}modifiers.push({groupId:ref.groupId,...(ref.refId?{refId:ref.refId}:{}),optionId:option.id,groupLabel:localText(resolved.label),optionLabel:localText(option.label),priceDelta:Number(option.priceDelta||0),...(hasPriceOverride?{priceOverride:Number(option.priceOverride||0)}:{})});});setSelectedOptions(selectedOptionsByGroup,ref,ids);});(product.comboSlots||[]).forEach((slot)=>{const ids=selectedChoiceIds(comboSelections.get(slot.id),defaultChoiceIds(slot));ids.forEach((id)=>{const choice=slot.choices.find((entry)=>entry.id===id);if(!choice)return;comboTotal+=Number(choice.priceDelta||0);comboChoices.push({slotLabel:localText(slot.label),choiceLabel:localText(choice.label),priceDelta:Number(choice.priceDelta||0)});});});const priceRule=matchingProductPriceRule(product,selectedOptionsByGroup);const coveredIndexes=modifiers.map((modifier,index)=>priceRuleCoversModifier(priceRule,modifier,product)?index:-1).filter((index)=>index>=0);const displayIndex=coveredIndexes.find((index)=>modifiers[index]?.groupId===priceRule?.displayGroupId)??coveredIndexes[0];modifierTotal=modifiers.reduce((sum,modifier)=>sum+(priceRuleCoversModifier(priceRule,modifier,product)?0:modifier.priceDelta),0);modifiers.forEach((modifier,index)=>{if(priceRuleCoversModifier(priceRule,modifier,product)){modifier.priceIncluded=true;modifier.priceDelta=0;delete modifier.priceOverride;if(index===displayIndex)modifier.variant=priceRule.priceOverride;}});const quantity=Math.max(1,Number(selection.quantity||1));const basePrice=priceRule?priceRule.priceOverride:typeof basePriceOverride==="number"?basePriceOverride:Number(product.basePrice||0);const unitTotal=basePrice+modifierTotal+comboTotal;return{product,quantity,unitTotal,lineTotal:unitTotal*quantity,modifiers,comboChoices,note:selection.note||""};}function activeMode(){return state.catalog.order.fulfillmentModes.find((mode)=>mode.id===state.cart.fulfillmentModeId);}function activeFees(){return(state.catalog.order.fees||[]).filter((fee)=>fee.enabled).filter((fee)=>!fee.appliesToModes||fee.appliesToModes.length===0||fee.appliesToModes.includes(state.cart.fulfillmentModeId)).map((fee)=>({label:localText(fee.label),amount:Number(fee.amount||0)}));}function cartTotals(){const lines=state.cart.items.map(priceLine).filter(Boolean);const fees=activeFees();const subtotal=lines.reduce((sum,line)=>sum+line.lineTotal,0);const feeTotal=fees.reduce((sum,fee)=>sum+fee.amount,0);return{lines,fees,subtotal,feeTotal,total:subtotal+feeTotal};}const shouldReduceMotion=()=>typeof window.matchMedia==="function"&&window.matchMedia("(prefers-reduced-motion: reduce)").matches;const useHoverListSpriteMode=()=>typeof window.matchMedia==="function"&&window.matchMedia("(hover: hover) and (pointer: fine)").matches;const sectionById=(id)=>state.catalog.sections.find((section)=>section.id===id);function presetSupportsAutoplay(presetId){return presetId==="loop-rail"||(!presetId.endsWith("-rail")&&presetId!=="thumbnails");}function presetSupportsAutoScroll(presetId){return presetId==="loop-rail"||presetId==="drag-free-rail"||presetId==="auto-scroll-rail";}function presentationAutoScrollEnabled(presentation){return presentation.presetId==="auto-scroll-rail"||Boolean(presentation.autoScroll&&presentation.autoScroll.enabled);}function presetUsesCenteredVisualFocus(presetId){return presetId==="drag-free-rail"||presetId==="auto-scroll-rail"||(!presetHasThumbnails(presetId)&&presetSupportsAutoplay(presetId));}function presetHasThumbnails(presetId){return/thumbnail/.test(presetId);}function presetUsesFade(presetId){return/^fade-/.test(presetId);}function buildEmblaOptions(presentation){switch(presentation.presetId){case"loop-rail":return{loop:true,align:"start"};case"contained-rail":return{containScroll:"trimSnaps",align:"start"};case"drag-free-rail":return{dragFree:true,containScroll:"trimSnaps",align:"start"};case"center-stage":case"thumbnail-hero":case"fade-thumbnails":case"fade-hero":case"scale-parallax-hero":return{loop:true,align:"center"};case"auto-scroll-rail":return{loop:true,dragFree:true,align:"start"};case"thumbnails":return{containScroll:"keepSnaps",align:"start"};default:return{align:"start"};}}function buildEmblaPlugins(presentation){const reduceMotion=shouldReduceMotion();const plugins=[];if(presetUsesFade(presentation.presetId)&&window.EmblaCarouselFade){plugins.push(window.EmblaCarouselFade());}if(!reduceMotion&&presetSupportsAutoplay(presentation.presetId)&&presentation.autoplay&&presentation.autoplay.enabled&&window.EmblaCarouselAutoplay){plugins.push(window.EmblaCarouselAutoplay({delay:presentation.autoplay.delayMs||4500,stopOnInteraction:true,stopOnMouseEnter:true}));}if(!reduceMotion&&presetSupportsAutoScroll(presentation.presetId)&&presentationAutoScrollEnabled(presentation)&&window.EmblaCarouselAutoScroll){plugins.push(window.EmblaCarouselAutoScroll({speed:presentation.autoScroll?.speed||1,stopOnInteraction:true,stopOnMouseEnter:true}));}return plugins;}function presentationNeedsEmblaVendor(presentation){const reduceMotion=shouldReduceMotion();return Boolean(presetUsesFade(presentation.presetId)||(!reduceMotion&&presetSupportsAutoplay(presentation.presetId)&&presentation.autoplay&&presentation.autoplay.enabled)||(!reduceMotion&&presetSupportsAutoScroll(presentation.presetId)&&presentationAutoScrollEnabled(presentation)));}function createNativeCarousel(viewport,presentation){const container=viewport?.querySelector?.(".catalog-carousel__container");const listeners=new Map();let selectedIndex=0;let scrollFrame=0;let destroyed=false;viewport.classList.add("is-native-carousel");const options=buildEmblaOptions(presentation);const isLoop=Boolean(options.loop);const isDragFree=Boolean(options.dragFree);const align=options.align==="center"?"center":"start";const slideNodes=()=>Array.from(container?.children||[]);const emit=(eventName)=>{(listeners.get(eventName)||[]).forEach((listener)=>listener());};const targetScrollLeftFor=(slide)=>{if(!slide)return 0;if(align==="center"){return slide.offsetLeft-Math.max(0,(viewport.clientWidth-slide.clientWidth)/2);}return slide.offsetLeft;};const nearestIndex=()=>{const slides=slideNodes();if(!slides.length)return 0;const anchor=viewport.scrollLeft+(align==="center"?viewport.clientWidth/2:0);return slides.map((slide,index)=>{const slideAnchor=slide.offsetLeft+(align==="center"?slide.clientWidth/2:0);return{index,distance:Math.abs(slideAnchor-anchor)};}).sort((left,right)=>left.distance-right.distance)[0]?.index??0;};const updateSelected=()=>{if(destroyed)return;const next=nearestIndex();const changed=next!==selectedIndex;selectedIndex=next;emit("scroll");if(changed)emit("select");};const queueUpdate=()=>{if(scrollFrame)return;scrollFrame=window.requestAnimationFrame?window.requestAnimationFrame(()=>{scrollFrame=0;updateSelected();}):window.setTimeout(()=>{scrollFrame=0;updateSelected();},16);};const scrollToIndex=(index,jump)=>{const slides=slideNodes();if(!slides.length)return;let next=Math.round(Number(index||0));if(isLoop)next=((next%slides.length)+slides.length)%slides.length;else next=Math.max(0,Math.min(slides.length-1,next));selectedIndex=next;const left=targetScrollLeftFor(slides[next]);viewport.scrollTo({left,behavior:jump||shouldReduceMotion()?"auto":"smooth"});updateSelected();};viewport.addEventListener("scroll",queueUpdate,{passive:true});if(isDragFree)viewport.classList.add("is-native-drag-free");window.setTimeout(()=>{updateSelected();emit("init");},0);return{slideNodes,selectedScrollSnap:()=>selectedIndex,scrollTo:scrollToIndex,scrollPrev:()=>scrollToIndex(selectedIndex-1),scrollNext:()=>scrollToIndex(selectedIndex+1),canScrollPrev:()=>isLoop||selectedIndex>0,canScrollNext:()=>isLoop||selectedIndex<slideNodes().length-1,on:(eventName,listener)=>{const list=listeners.get(eventName)||[];list.push(listener);listeners.set(eventName,list);},destroy:()=>{destroyed=true;viewport.removeEventListener("scroll",queueUpdate);viewport.classList.remove("is-native-carousel");viewport.classList.remove("is-native-drag-free");if(scrollFrame){if(window.cancelAnimationFrame)window.cancelAnimationFrame(scrollFrame);else window.clearTimeout(scrollFrame);}listeners.clear();}};}const posterBytesFor=(image)=>Math.max(0,Number(image?.getAttribute("data-poster-bytes")||0));function ensurePosterLoaded(image){if(!image)return"";const actual=image.getAttribute("data-poster-src")||"";const current=image.getAttribute("src")||"";if(!actual)return current;if(image.dataset.posterLoaded==="true")return actual;image.setAttribute("src",actual);image.dataset.posterLoaded="true";return actual;}function schedulePosterCacheWarm(image){if(!image||image.dataset.posterCacheScheduled==="true")return;const src=image.getAttribute("data-poster-src")||image.getAttribute("src")||"";if(!src)return;image.dataset.posterCacheScheduled="true";const runner=()=>{const bytes=posterBytesFor(image);const warm=()=>{void warmAssetInCache(src,"list",bytes);};if(typeof window.requestIdleCallback==="function"){window.requestIdleCallback(warm,{timeout:3000});return;}window.setTimeout(warm,240);};if(image.complete&&image.naturalWidth>0){runner();return;}image.addEventListener("load",runner,{once:true});}function ensureCarouselDependencies(presentation){const reduceMotion=shouldReduceMotion();if(!presentationNeedsEmblaVendor(presentation))return Promise.resolve([]);const loaders=[];if(!window.EmblaCarousel){loaders.push(ensureOptionalVendor("EmblaCarousel","embla-carousel.js"));}if(presetUsesFade(presentation.presetId)&&!window.EmblaCarouselFade){loaders.push(ensureOptionalVendor("EmblaCarouselFade","embla-fade.js"));}if(!reduceMotion&&presetSupportsAutoplay(presentation.presetId)&&presentation.autoplay&&presentation.autoplay.enabled&&!window.EmblaCarouselAutoplay){loaders.push(ensureOptionalVendor("EmblaCarouselAutoplay","embla-autoplay.js"));}if(!reduceMotion&&presetSupportsAutoScroll(presentation.presetId)&&presentationAutoScrollEnabled(presentation)&&!window.EmblaCarouselAutoScroll){loaders.push(ensureOptionalVendor("EmblaCarouselAutoScroll","embla-auto-scroll.js"));}return loaders.length?Promise.all(loaders):Promise.resolve([]);}function ensureCarouselInstance(sectionElement){const sectionId=sectionElement?.getAttribute?.("data-section-id")||"";if(!sectionId||carouselInstances.has(sectionId))return;const section=sectionById(sectionId);const viewport=sectionElement.querySelector("[data-carousel-viewport]");if(!section||!viewport)return;ensureCarouselDependencies(section.presentation).then(()=>{const useNative=!presentationNeedsEmblaVendor(section.presentation);if(!sectionElement.isConnected||carouselInstances.has(section.id)||(!useNative&&!window.EmblaCarousel))return;const embla=useNative?createNativeCarousel(viewport,section.presentation):window.EmblaCarousel(viewport,buildEmblaOptions(section.presentation),buildEmblaPlugins(section.presentation));const entry={embla,sectionId:section.id,sectionElement,viewport,visible:visibleCarouselSectionIds.has(section.id)};carouselInstances.set(section.id,entry);const update=()=>{updateCarouselState(entry,section.presentation);if(entry.visible)loadCarouselNeighborhood(entry);};const updateAndQueue=()=>{update();queueFocusedCarouselUpdate();};embla.on("init",updateAndQueue);embla.on("select",updateAndQueue);embla.on("reInit",updateAndQueue);if(presetUsesCenteredVisualFocus(section.presentation.presetId)){embla.on("scroll",updateAndQueue);}sectionElement.setAttribute("data-carousel-ready","true");const savedIndex=carouselSelectedIndexBySection.get(section.id);if(Number.isFinite(savedIndex)&&savedIndex>0){embla.scrollTo(savedIndex,true);}updateAndQueue();});}function destroyCarousels(){if(activeListSpriteController){activeListSpriteController.deactivate();activeListSpriteController=null;}focusedCarouselSectionId="";focusedCarouselSectionPinnedUntil=0;focusedCarouselSectionPinnedScrollY=0;carouselInstances.forEach((entry)=>{carouselSelectedIndexBySection.set(entry.sectionId,entry.embla.selectedScrollSnap());entry.embla.destroy();});carouselInstances.clear();}function ensureListSpriteController(canvas){if(!canvas)return null;const existing=listSpriteControllerByCanvas.get(canvas);if(existing)return existing;const media=canvas.closest(".catalog-card__media");const src=canvas.getAttribute("data-sprite360-src")||"";const role=canvas.getAttribute("data-sprite360-role")||"listSprite360";const bytes=Math.max(0,Number(canvas.getAttribute("data-sprite360-bytes")||0));if(!media||!src)return null;const frameMeta={frameCount:Math.max(2,Number(canvas.getAttribute("data-sprite360-frame-count")||0)),columns:Math.max(1,Number(canvas.getAttribute("data-sprite360-columns")||0)),rows:Math.max(1,Number(canvas.getAttribute("data-sprite360-rows")||0)),frameWidth:Math.max(1,Number(canvas.getAttribute("data-sprite360-frame-width")||0)),frameHeight:Math.max(1,Number(canvas.getAttribute("data-sprite360-frame-height")||0)),fps:Math.max(1,Number(canvas.getAttribute("data-sprite360-fps")||12))};let frameIndex=0;let intervalId=0;let playback=null;let loadTask=null;const setState=(value)=>{media.dataset.listSpriteState=value;media.classList.toggle("is-list-sprite-active",value==="active");};const stopLoop=()=>{if(!intervalId)return;window.clearInterval(intervalId);intervalId=0;};const controller={canvas,media,activate(){const connectionPolicy=readConnectionPolicy();if(!connectionPolicy.allowListSprite360||!canvas.isConnected){controller.deactivate();return;}if(activeListSpriteController!==controller)return;const beginSpriteLoad=()=>{if(!canvas.isConnected||activeListSpriteController!==controller)return;setState("loading");media.classList.remove("is-sprite-failed");const task=loadTask||loadSprite360Image(src,{bucket:role==="sprite360"?"detail":"list",bytes}).then((image)=>{const context=canvas.getContext("2d",{alpha:true,willReadFrequently:true});if(!context)throw new Error("sprite360 canvas unavailable");const resolved=resolveSpritePlaybackFrames(src,image,frameMeta,context);canvas.width=resolved.frame.frameWidth;canvas.height=resolved.frame.frameHeight;return{context,image,frame:resolved.frame,visibleFrameIndexes:resolved.visibleFrameIndexes};});loadTask=task;task.then((resolved)=>{if(!canvas.isConnected||activeListSpriteController!==controller)return;playback=resolved;frameIndex=0;if(!renderVisibleSpriteFrame(resolved.context,resolved.image,resolved.frame,resolved.visibleFrameIndexes,frameIndex)){media.classList.add("is-loaded","is-sprite-failed");setState("failed");return;}media.dataset.listSpriteFrame=String(resolveVisibleSpriteFrameIndex(resolved.visibleFrameIndexes,frameIndex));media.classList.add("is-loaded","has-sprite");setState("active");stopLoop();if(window.matchMedia("(prefers-reduced-motion: reduce)").matches)return;intervalId=window.setInterval(()=>{if(!canvas.isConnected||activeListSpriteController!==controller){stopLoop();return;}frameIndex+=1;renderVisibleSpriteFrame(resolved.context,resolved.image,resolved.frame,resolved.visibleFrameIndexes,frameIndex);media.dataset.listSpriteFrame=String(resolveVisibleSpriteFrameIndex(resolved.visibleFrameIndexes,frameIndex));},Math.max(60,1000/resolved.frame.fps));}).catch(()=>{media.classList.add("is-loaded","is-sprite-failed");setState("failed");});};const poster=media.querySelector(".catalog-card__image");ensurePosterLoaded(poster);if(poster&&(!poster.complete||poster.naturalWidth<=0)){poster.addEventListener("load",beginSpriteLoad,{once:true});poster.addEventListener("error",beginSpriteLoad,{once:true});return;}beginSpriteLoad();},deactivate(){stopLoop();if(activeListSpriteController===controller){activeListSpriteController=null;}if(!media.isConnected)return;setState("poster");media.dataset.listSpriteFrame="";if(playback){playback.context.clearRect(0,0,playback.frame.frameWidth,playback.frame.frameHeight);}else{const context=canvas.getContext("2d");context?.clearRect(0,0,canvas.width||0,canvas.height||0);}}};setState("poster");listSpriteControllerByCanvas.set(canvas,controller);return controller;}function setActiveListSprite(canvas){const connectionPolicy=readConnectionPolicy();const nextController=connectionPolicy.allowListSprite360?ensureListSpriteController(canvas):null;if(activeListSpriteController===nextController)return;if(activeListSpriteController&&activeListSpriteController!==nextController){activeListSpriteController.deactivate();}activeListSpriteController=nextController;activeListSpriteController?.activate();}function carouselNeighborIndexes(embla){const slides=embla.slideNodes();const selected=embla.selectedScrollSnap();const indexes=new Set([selected]);const radius=readConnectionPolicy().listNeighborRadius;for(let offset=1;offset<=radius;offset+=1){if(slides[selected-offset])indexes.add(selected-offset);if(slides[selected+offset])indexes.add(selected+offset);}return[...indexes];}function carouselVisibleIndexes(entry){const viewport=entry.viewport;const slides=entry.embla.slideNodes();if(!viewport||!slides.length)return carouselNeighborIndexes(entry.embla);const viewportRect=viewport.getBoundingClientRect();const horizontalInset=Math.min(24,Math.max(8,viewportRect.width*0.04));const indexes=[];slides.forEach((slide,index)=>{const rect=slide.getBoundingClientRect();const intersectsHorizontally=rect.right>=viewportRect.left+horizontalInset&&rect.left<=viewportRect.right-horizontalInset;const intersectsVertically=rect.bottom>=viewportRect.top&&rect.top<=viewportRect.bottom;if(intersectsHorizontally&&intersectsVertically){indexes.push(index);}});return indexes.length?indexes:carouselNeighborIndexes(entry.embla);}function carouselFocusedIndex(entry){const slides=entry.embla.slideNodes();if(!slides.length)return entry.embla.selectedScrollSnap();const visibleIndexes=carouselVisibleIndexes(entry);const viewport=entry.viewport;if(!viewport)return visibleIndexes[0]??entry.embla.selectedScrollSnap();const viewportRect=viewport.getBoundingClientRect();const viewportCenter=viewportRect.left+viewportRect.width/2;const candidates=(visibleIndexes.length?visibleIndexes:slides.map((_,index)=>index)).map((index)=>{const rect=slides[index]?.getBoundingClientRect();const center=rect?rect.left+rect.width/2:viewportCenter;return{index,distance:Math.abs(center-viewportCenter)};}).sort((left,right)=>left.distance-right.distance);return candidates[0]?.index??entry.embla.selectedScrollSnap();}function carouselPosterPreloadIndexes(entry){const indexes=new Set(carouselVisibleIndexes(entry));const slides=entry.embla.slideNodes();const focusIndex=carouselActiveIndex(entry);const radius=Math.max(1,readConnectionPolicy().listNeighborRadius);if(slides[focusIndex])indexes.add(focusIndex);for(let offset=1;offset<=radius;offset+=1){if(slides[focusIndex-offset])indexes.add(focusIndex-offset);if(slides[focusIndex+offset])indexes.add(focusIndex+offset);}if(!indexes.size)indexes.add(entry.embla.selectedScrollSnap());return[...indexes];}function loadCarouselNeighborhood(entry){carouselPosterPreloadIndexes(entry).forEach((index)=>{const slide=entry.embla.slideNodes()[index];const poster=slide?.querySelector(".catalog-card__image");const posterSrc=ensurePosterLoaded(poster);if(posterSrc)schedulePosterCacheWarm(poster);const thumbPoster=entry.sectionElement.querySelector('[data-carousel-thumb][data-slide-index="'+index+'"] img');ensurePosterLoaded(thumbPoster);});}function loadCarouselThumbnails(entry){entry.sectionElement.querySelectorAll("[data-carousel-thumb] img").forEach((image)=>{const posterSrc=ensurePosterLoaded(image);if(posterSrc)schedulePosterCacheWarm(image);});}function syncCategoryNavActive(sectionId){const activeSection=sectionById(sectionId);app.querySelectorAll(".catalog-category-pill").forEach((pill)=>{const targetId=pill.getAttribute("data-scroll-target-id")||"";const isActive=Boolean(activeSection&&targetId===sectionDomId(activeSection));pill.classList.toggle("is-active",isActive);if(isActive)pill.setAttribute("aria-current","true");else pill.removeAttribute("aria-current");});}function carouselActiveIndex(entry){const presetId=sectionById(entry.sectionId)?.presentation?.presetId||"";return presetUsesCenteredVisualFocus(presetId)?carouselFocusedIndex(entry):entry.embla.selectedScrollSnap();}function syncSelectedThumbnail(entry,focusIndex){const selectedThumb=entry.sectionElement.querySelector('[data-carousel-thumb][data-slide-index="'+focusIndex+'"]');const rail=selectedThumb?.parentElement;if(!selectedThumb||!rail)return;const railRect=rail.getBoundingClientRect();const thumbRect=selectedThumb.getBoundingClientRect();if(thumbRect.left<railRect.left){rail.scrollLeft-=railRect.left-thumbRect.left+8;}else if(thumbRect.right>railRect.right){rail.scrollLeft+=thumbRect.right-railRect.right+8;}}function updateCarouselState(entry,presentation){const selected=entry.embla.selectedScrollSnap();const focusIndex=presetUsesCenteredVisualFocus(presentation.presetId)?carouselFocusedIndex(entry):selected;carouselSelectedIndexBySection.set(entry.sectionId,selected);entry.sectionElement.querySelectorAll("[data-carousel-thumb]").forEach((thumb)=>{thumb.classList.toggle("is-selected",Number(thumb.getAttribute("data-slide-index"))===focusIndex);thumb.setAttribute("aria-selected",Number(thumb.getAttribute("data-slide-index"))===focusIndex?"true":"false");});if(presetHasThumbnails(presentation.presetId))syncSelectedThumbnail(entry,focusIndex);entry.embla.slideNodes().forEach((slide,index)=>{slide.classList.toggle("is-selected",index===focusIndex);if(presentation.presetId==="scale-parallax-hero"){const distance=Math.min(2,Math.abs(index-selected));slide.style.setProperty("--catalog-slide-scale",String(1-distance*0.08));slide.style.setProperty("--catalog-slide-offset",String((index-selected)*-18)+"px");}});}function markCarouselVisible(sectionElement){const entry=carouselInstances.get(sectionElement.getAttribute("data-section-id"));if(!entry)return;entry.visible=true;}function queueFocusedCarouselUpdate(){if(carouselFocusUpdateQueued)return;carouselFocusUpdateQueued=true;const runner=()=>{carouselFocusUpdateQueued=false;if(state.detailOpen){setActiveListSprite(null);return;}const visibleEntries=Array.from(carouselInstances.values()).filter((entry)=>visibleCarouselSectionIds.has(entry.sectionElement.getAttribute("data-section-id")));const candidates=visibleEntries.length?visibleEntries:Array.from(carouselInstances.values()).slice(0,1);if(!candidates.length){setActiveListSprite(null);return;}const viewportCenter=window.innerHeight*0.42;const rankedCandidates=candidates.map((entry)=>{const rect=entry.sectionElement.getBoundingClientRect();const anchor=rect.top+Math.min(rect.height*0.38,220);return{entry,distance:Math.abs(anchor-viewportCenter)};}).sort((left,right)=>left.distance-right.distance);const pinnedCandidate=Date.now()<focusedCarouselSectionPinnedUntil?rankedCandidates.find((candidate)=>candidate.entry.sectionId===focusedCarouselSectionId):null;const pinnedEntry=pinnedCandidate&&Math.abs(window.scrollY-focusedCarouselSectionPinnedScrollY)<=24?pinnedCandidate.entry:null;const focusedEntry=pinnedEntry||rankedCandidates[0]?.entry;if(!focusedEntry){setActiveListSprite(null);return;}focusedCarouselSectionId=focusedEntry.sectionElement.getAttribute("data-section-id")||focusedCarouselSectionId;loadCarouselNeighborhood(focusedEntry);if(presetHasThumbnails(sectionById(focusedEntry.sectionId)?.presentation?.presetId)){loadCarouselThumbnails(focusedEntry);}syncCategoryNavActive(focusedEntry.sectionId);if(useHoverListSpriteMode()){const hoveredCard=app.querySelector("[data-product-card]:hover");setActiveListSprite(hoveredCard?.querySelector("[data-sprite360-src]")||null);scheduleBackgroundWarmup();return;}const activeSlide=focusedEntry.embla.slideNodes()[carouselActiveIndex(focusedEntry)];setActiveListSprite(activeSlide?.querySelector("[data-sprite360-src]"));scheduleBackgroundWarmup();};if(typeof window.requestAnimationFrame==="function"){window.requestAnimationFrame(runner);return;}window.setTimeout(runner,16);}function initCarousels(){const firstSection=app.querySelector("[data-carousel-section]");if(firstSection)ensureCarouselInstance(firstSection);}function badgeEntries(product){return[...(product.marketingBadges||[]),...(product.dietaryBadges||[])];}function allergenKey(allergen){return String(allergen.id||textOf(allergen.label,state.catalog.meta.defaultLocale,state.catalog.meta.defaultLocale)).trim().toLowerCase();}function allergenEntries(product){return(product.allergens||[]).map((allergen)=>({id:allergenKey(allergen),label:localText(allergen.label)})).filter((allergen)=>allergen.id&&allergen.label);}function productMatches(product){if(!product.available&&product.unavailableMode==="hidden")return false;if(state.filters.size>0){const ids=new Set(badgeEntries(product).map((badge)=>badge.id));for(const filter of state.filters){if(!ids.has(filter))return false;}}if(state.allergenFilters.size>0){const ids=new Set(allergenEntries(product).map((allergen)=>allergen.id));for(const filter of state.allergenFilters){if(ids.has(filter))return false;}}return true;}function allBadgeFilters(){const filters=new Map();state.catalog.products.forEach((product)=>{badgeEntries(product).forEach((badge)=>{if(!filters.has(badge.id)){filters.set(badge.id,{id:badge.id,intent:badge.intent,label:localText(badge.label)||badge.id});}});});return[...filters.values()];}function allAllergenFilters(){const filters=new Map();state.catalog.products.forEach((product)=>{allergenEntries(product).forEach((allergen)=>{if(!filters.has(allergen.id)){filters.set(allergen.id,allergen);}});});return[...filters.values()];}function renderBadges(product){return badgeEntries(product).map((badge)=>{const label=localText(badge.label)||badge.id;return'<span class="catalog-badge catalog-badge--'+escapeHtml(badge.intent)+'">'+escapeHtml(label)+'</span>';}).join("");}function renderSpiceLevel(value){const level=Math.max(0,Math.min(5,Number(value||0)));if(!level)return"";return'<span class="catalog-spice">'+[0,1,2,3,4].map((index)=>'<span class="catalog-spice__flame'+(index<level?' is-active':'')+'" aria-hidden="true">'+icon("flame")+'</span>').join("")+'</span>';}function renderSpice(product){return renderSpiceLevel(product.spiceLevel);}function renderAllergens(product){const allergens=allergenEntries(product);if(!allergens.length)return"";return'<span class="catalog-allergens">'+allergens.map((allergen)=>'<span class="catalog-allergen">'+escapeHtml(allergen.label)+'</span>').join("")+'</span>';}function renderOptionMeta(option){const description=localText(option.description);const parts=[description,...(option.dietaryBadges||[]).map((badge)=>localText(badge.label)||badge.id),...(option.allergens||[]).map((allergen)=>localText(allergen.label))].filter(Boolean);const level=Math.max(0,Math.min(5,Number(option.spiceLevel||0)));return parts.length||level?'<small>'+(parts.length?'<span>'+escapeHtml(parts.join(" · "))+'</span>':'')+renderSpiceLevel(level)+'</small>':"";}function renderDeliveryLinks(){const links=(state.catalog.order.deliveryLinks||[]).map((link)=>({label:localText(link.label),url:safeHttpsUrl(link.url),iconPath:link.icon?.path||"",enabled:link.enabled!==false})).filter((link)=>link.enabled&&link.label&&link.url);if(!links.length)return"";return'<div class="catalog-delivery-block"><p>'+escapeHtml(copy("ad"))+'</p><div class="catalog-delivery-links">'+links.map((link)=>'<a class="catalog-delivery-link'+(link.iconPath?' catalog-delivery-link--icon':'')+'" href="'+escapeHtml(link.url)+'" target="_blank" rel="noopener" aria-label="'+escapeHtml(link.label)+'">'+(link.iconPath?'<img src="'+escapeHtml(assetSource(link.iconPath))+'" alt="'+escapeHtml(link.label)+'" loading="lazy" decoding="async" />':icon("cart")+'<span>'+escapeHtml(link.label)+'</span>')+'</a>').join("")+'</div></div>';}function renderFaqs(){const entries=(state.catalog.business.faqs||[]).map((faq)=>({question:localText(faq.question),answer:localText(faq.answer)})).filter((faq)=>faq.question&&faq.answer);if(!entries.length)return"";return'<section class="catalog-faq"><h3>'+escapeHtml(copy("q"))+'</h3>'+entries.map((faq,index)=>'<details data-faq'+(index===0?' open':'')+'><summary>'+escapeHtml(faq.question)+'</summary><div>'+renderMarkdown(faq.answer)+'</div></details>').join("")+'</section>';}function sectionDomId(section){const raw=String(section.id||localText(section.title)||"section");const safe=raw.toLowerCase().replace(/[^a-z0-9_-]+/g,"-").replace(/^-+|-+$/g,"");return"catalog-section-"+(safe||"section");}function sectionInitial(section){const label=localText(section.title)||section.id||"";return label.trim().slice(0,1).toUpperCase()||"#";}function sectionNavIcon(section){const iconName=String(section.icon||"").trim();const iconSvg=iconName?icon(iconName):"";return iconSvg||escapeHtml(sectionInitial(section));}function businessLocations(){const locations=state.catalog?.business?.locations;if(!Array.isArray(locations))return[];return locations.filter((location)=>location&&localText(location.label)&&localText(location.address));}function renderLocation(location){const label=localText(location.label);const address=localText(location.address);const description=localText(location.description);const hours=localText(location.hours);const phone=String(location.phone||"").trim();const mapsUrl=safeHttpsUrl(location.mapsUrl);const embedUrl=safeHttpsUrl(location.embedUrl);const locationId=String(location.id||label||address);const hasMap=Boolean(embedUrl);return'<article class="catalog-location-card'+(hasMap?' catalog-location-card--with-map':'')+'" data-location-id="'+escapeHtml(locationId)+'">'+'<div class="catalog-location-card__copy"><h3>'+escapeHtml(label)+'</h3>'+(!hasMap?'<p class="catalog-location-card__address">'+escapeHtml(address)+'</p>':'')+(description?'<p>'+escapeHtml(description)+'</p>':'')+(hours?'<p class="catalog-location-card__meta"><span>'+escapeHtml(copy("w"))+'</span>'+escapeHtml(hours)+'</p>':'')+(phone?'<p class="catalog-location-card__meta"><span>'+escapeHtml(copy("O"))+'</span>'+escapeHtml(phone)+'</p>':'')+(mapsUrl?'<a class="catalog-location-card__cta" href="'+escapeHtml(mapsUrl)+'" target="_blank" rel="noreferrer">'+escapeHtml(copy("t"))+'</a>':'')+'</div>'+(hasMap?'<div class="catalog-location-card__map"><iframe title="'+escapeHtml(label+" map")+'" src="'+escapeHtml(embedUrl)+'" loading="lazy" referrerpolicy="no-referrer-when-downgrade" allowfullscreen></iframe></div>':'<div class="catalog-location-card__static"><strong>'+escapeHtml(address)+'</strong><span>'+escapeHtml(copy("C"))+'</span></div>')+'</article>';}function renderLandingPage(){const businessName=localText(state.catalog.business.name);const logoPath=state.catalog.business?.logo?.path||"";const logoHtml=logoPath?'<img class="catalog-landing__logo" src="'+escapeHtml(assetSource(logoPath))+'" alt="'+escapeHtml(businessName)+'" />':'<div class="catalog-landing__logo catalog-landing__logo--fallback" aria-hidden="true">'+icon("foodMenu")+'</div>';return'<main id="catalog-home" class="catalog-page catalog-page--home">'+'<section class="catalog-landing">'+'<div class="catalog-landing__copy">'+logoHtml+'<a class="catalog-home-cta catalog-primary catalog-primary--link" href="#menu">'+buttonContent("foodMenu",copy("B"))+'</a>'+'</div>'+'</section>'+'</main>';}function renderAboutPage(){const aboutMarkdown=localText(state.catalog.business.aboutMarkdown);const legal=aboutMarkdown||localText(state.catalog.business.legalDisclaimer);const aboutTitle=localText(state.catalog.business.aboutTitle)||copy("d");return'<main id="catalog-about" class="catalog-page catalog-page--about">'+'<section class="catalog-info-page">'+'<h2>'+escapeHtml(aboutTitle)+'</h2>'+(legal?'<div class="catalog-info-page__markdown">'+(aboutMarkdown?renderMarkdown(legal):escapeHtml(legal))+'</div>':'')+renderFaqs()+'</section>'+'</main>';}function renderContactPage(){const locations=businessLocations();const contactMarkdown=localText(state.catalog.business.contactMarkdown);const contactTitle=localText(state.catalog.business.contactTitle)||copy("u");return'<main id="catalog-contact" class="catalog-page catalog-page--contact">'+'<section class="catalog-info-page">'+'<h2>'+escapeHtml(contactTitle)+'</h2>'+(contactMarkdown?'<div class="catalog-info-page__markdown">'+renderMarkdown(contactMarkdown)+'</div>':'')+(locations.length?'<div class="catalog-locations__grid">'+locations.map(renderLocation).join("")+'</div>':'<strong class="catalog-empty">'+escapeHtml(copy("C"))+'</strong>')+'</section>'+'</main>';}function spriteDataAttrs(sprite,sectionId,productId,role){const runtimeAsset=runtimeAssetFor(role,sectionId,productId,sprite.path);const bytes=Number(runtimeAsset?.bytes||sprite.bytes||0);return' data-sprite360-src="'+escapeHtml(assetSource(sprite.path,sectionId,productId,role))+'"'+' data-sprite360-role="'+escapeHtml(role)+'"'+' data-sprite360-frame-count="'+escapeHtml(sprite.frameCount)+'"'+' data-sprite360-columns="'+escapeHtml(sprite.columns)+'"'+' data-sprite360-rows="'+escapeHtml(sprite.rows)+'"'+' data-sprite360-frame-width="'+escapeHtml(sprite.frameWidth)+'"'+' data-sprite360-frame-height="'+escapeHtml(sprite.frameHeight)+'"'+' data-sprite360-fps="'+escapeHtml(sprite.fps||12)+'"'+' data-sprite360-bytes="'+escapeHtml(bytes)+'"'+' data-sprite360-rotation-direction="'+escapeHtml(sprite.rotationDirection||"cw")+'"';}function detailHqSpriteDataAttrs(sprite,sectionId,productId){if(!sprite?.path)return"";const runtimeAsset=runtimeAssetFor("sprite360",sectionId,productId,sprite.path);const bytes=Number(runtimeAsset?.bytes||sprite.bytes||0);return' data-detail-hq-src="'+escapeHtml(assetSource(sprite.path,sectionId,productId,"sprite360"))+'"'+' data-detail-hq-frame-count="'+escapeHtml(sprite.frameCount)+'"'+' data-detail-hq-columns="'+escapeHtml(sprite.columns)+'"'+' data-detail-hq-rows="'+escapeHtml(sprite.rows)+'"'+' data-detail-hq-frame-width="'+escapeHtml(sprite.frameWidth)+'"'+' data-detail-hq-frame-height="'+escapeHtml(sprite.frameHeight)+'"'+' data-detail-hq-fps="'+escapeHtml(sprite.fps||12)+'"'+' data-detail-hq-bytes="'+escapeHtml(bytes)+'"'+' data-detail-hq-rotation-direction="'+escapeHtml(sprite.rotationDirection||"cw")+'"';}function detailSpriteDataAttrs(product,sectionId){const listSprite=product.media.listSprite360;const detailSprite=product.media.sprite360;const previewSprite=listSprite?.path?listSprite:detailSprite;if(!previewSprite?.path)return"";const previewRole=listSprite?.path?"listSprite360":"sprite360";const hqAttrs=detailSprite?.path&&detailSprite.path!==previewSprite.path?detailHqSpriteDataAttrs(detailSprite,sectionId,product.id):"";return spriteDataAttrs(previewSprite,sectionId,product.id,previewRole)+hqAttrs;}function listSpriteForProduct(product){if(product.media.listSprite360?.path){return{sprite:product.media.listSprite360,origin:"list"};}if(product.media.sprite360?.path){return{sprite:product.media.sprite360,origin:"detail"};}return{sprite:null,origin:""};}function renderCard(section,product){const name=localText(product.name);const description=localText(product.description);const disabled=!product.available;const listSprite=listSpriteForProduct(product);const sprite=listSprite.sprite;const hasSprite=Boolean(sprite&&sprite.path);const posterSrc=assetSource(product.media.poster.path,section.id,product.id,"poster");const posterBytes=Math.max(0,Number(product.media.poster.bytes||0));return'<article class="catalog-card catalog-carousel__slide'+(disabled?' is-disabled':'')+(hasSprite?' has-360':'')+'" data-product-card="'+escapeHtml(product.id)+'" data-product-section-id="'+escapeHtml(section.id)+'" data-has-sprite360="'+(hasSprite?"true":"false")+'">'+'<button class="catalog-card__button" type="button" data-action="open-product" data-product-id="'+escapeHtml(product.id)+'" data-product-section-id="'+escapeHtml(section.id)+'"'+(disabled?' aria-disabled="true"':'')+'>'+'<span class="catalog-card__media'+(hasSprite?' has-360-source':'')+'"'+(hasSprite?' data-list-sprite-state="poster" data-list-sprite-origin="'+escapeHtml(listSprite.origin)+'"':'')+'>'+'<img class="catalog-card__image" src="'+posterPlaceholderSrc+'" data-poster-src="'+escapeHtml(posterSrc)+'" data-poster-bytes="'+escapeHtml(posterBytes)+'" alt="" decoding="async" />'+(hasSprite?'<canvas class="catalog-card__sprite" aria-hidden="true"'+spriteDataAttrs(sprite,section.id,product.id,"listSprite360")+'></canvas>'+'<span class="catalog-card__loader" aria-hidden="true"></span>'+'<span class="catalog-360-badge">'+escapeHtml(copy("ab"))+'</span>'+'<span class="catalog-card__sprite-status">'+escapeHtml(copy("U"))+'</span>':'')+'</span>'+'<span class="catalog-card__content">'+'<span class="catalog-card__badges">'+renderBadges(product)+renderSpice(product)+'</span>'+'<span class="catalog-card__title">'+escapeHtml(name)+'</span>'+(description?'<span class="catalog-card__description">'+escapeHtml(description)+'</span>':'')+'<span class="catalog-card__meta">'+escapeHtml(productPriceLabel(product,"card"))+(disabled?' - '+escapeHtml(copy("Y")):'')+'</span>'+'</span></button>'+'<button class="catalog-card__quick-add catalog-primary" type="button" data-action="add-default-product" data-product-id="'+escapeHtml(product.id)+'" data-product-section-id="'+escapeHtml(section.id)+'" aria-label="'+escapeHtml(copy("a")+" "+name)+'"'+(disabled?' disabled':'')+'>'+escapeHtml(copy("a"))+'</button>'+'</article>';}function renderThumbnails(section,products){if(!presetHasThumbnails(section.presentation.presetId))return"";return'<div class="catalog-thumbnails" aria-label="'+escapeHtml(localText(section.title))+' thumbnails">'+products.map((product,index)=>{const name=localText(product.name);const posterSrc=assetSource(product.media.poster.path,section.id,product.id,"poster");const posterBytes=Math.max(0,Number(product.media.poster.bytes||0));return'<button type="button" data-action="carousel-thumb" data-section-id="'+escapeHtml(section.id)+'" data-slide-index="'+index+'" data-carousel-thumb>'+'<img src="'+posterPlaceholderSrc+'" data-poster-src="'+escapeHtml(posterSrc)+'" data-poster-bytes="'+escapeHtml(posterBytes)+'" alt="" decoding="async" />'+'<span>'+escapeHtml(name)+'</span>'+'</button>';}).join("")+'</div>';}function renderSections(){const sections=state.catalog.sections.map((section)=>{const products=section.productIds.map(productById).filter(Boolean).filter(productMatches);if(!products.length)return"";return'<section id="'+escapeHtml(sectionDomId(section))+'" class="catalog-section catalog-section--'+escapeHtml(section.presentation.presetId)+'" data-carousel-section data-section-id="'+escapeHtml(section.id)+'" data-preset="'+escapeHtml(section.presentation.presetId)+'">'+'<div class="catalog-section__heading"><h2>'+escapeHtml(localText(section.title))+'</h2>'+'<span class="catalog-section__preset">'+escapeHtml(products.length+" "+(products.length===1?copy("y"):copy("z")))+'</span></div>'+'<div class="catalog-carousel">'+'<div class="catalog-carousel__viewport" data-carousel-viewport><div class="catalog-rail catalog-carousel__container">'+products.map((product)=>renderCard(section,product)).join("")+'</div></div>'+'<div class="catalog-carousel__controls">'+'<button class="catalog-icon-button" type="button" data-action="carousel-prev" data-section-id="'+escapeHtml(section.id)+'" aria-label="'+escapeHtml(copy("L"))+'">'+icon("chevronLeft")+'</button>'+'<button class="catalog-icon-button" type="button" data-action="carousel-next" data-section-id="'+escapeHtml(section.id)+'" aria-label="'+escapeHtml(copy("I"))+'">'+icon("chevronRight")+'</button>'+'</div>'+renderThumbnails(section,products)+'</div></section>';}).filter(Boolean);return sections.length?sections.join(""):'<section class="catalog-empty-state"><strong>'+escapeHtml(copy("G"))+'</strong></section>';}function renderCategoryNav(){const sections=state.catalog.sections.filter((section)=>section.productIds.map(productById).filter(Boolean).length);if(!sections.length)return"";const activeSection=sectionById(focusedCarouselSectionId)||sections[0];return'<nav class="catalog-category-nav" aria-label="'+escapeHtml(copy("R"))+'">'+sections.map((section)=>{const title=localText(section.title);const longClass=title.length>11?' is-long-label':'';const targetId=sectionDomId(section);const activeClass=activeSection&&activeSection.id===section.id?' is-active':'';return'<a class="catalog-category-pill'+longClass+activeClass+'" href="#'+escapeHtml(targetId)+'" title="'+escapeHtml(title)+'" data-action="scroll-target" data-scroll-target-id="'+escapeHtml(targetId)+'"'+(activeClass?' aria-current="true"':'')+'>'+'<span class="catalog-category-pill__content"><span class="catalog-category-pill__icon">'+sectionNavIcon(section)+'</span>'+'<span class="catalog-category-pill__label"><span class="catalog-category-pill__label-text">'+escapeHtml(title)+'</span></span></span>'+'</a>';}).join("")+'</nav>';}function renderFilters(){const badgeButtons=allBadgeFilters().map((filter)=>{const active=state.filters.has(filter.id);return'<button class="catalog-filter'+(active?' is-active':'')+'" type="button" data-action="toggle-filter" data-filter-id="'+escapeHtml(filter.id)+'">'+escapeHtml(filter.label)+'</button>';}).join("");const allergenButtons=allAllergenFilters().map((filter)=>{const active=state.allergenFilters.has(filter.id);return'<button class="catalog-filter catalog-filter--allergen'+(active?' is-active':'')+'" type="button" data-action="toggle-allergen-filter" data-allergen-filter-id="'+escapeHtml(filter.id)+'">'+escapeHtml(copy("ac")+" "+filter.label)+'</button>';}).join("");return[badgeButtons?'<div class="catalog-filter-group"><span class="catalog-filter-group__label">'+escapeHtml(copy("aa"))+'</span><div class="catalog-filters">'+badgeButtons+'</div></div>':"",allergenButtons?'<div class="catalog-filter-group"><span class="catalog-filter-group__label">'+escapeHtml(copy("c"))+'</span><div class="catalog-filters">'+allergenButtons+'</div></div>':""].join("");}function renderMenuControls(){const filters=renderFilters();return filters?'<section class="catalog-controls"><div class="catalog-filter-groups">'+filters+'</div></section>':"";}function renderMenuPage(){return'<main id="catalog-menu" class="catalog-page catalog-page--menu">'+renderCategoryNav()+renderMenuControls()+'<div class="catalog-content">'+renderSections()+'</div>'+'</main>';}function renderSiteNav(){const entries=[{id:"home",icon:"home"},{id:"menu",icon:"foodMenu"},{id:"about",icon:"info"},{id:"contact",icon:"mail"}];return'<button class="catalog-nav-backdrop'+(state.navOpen?' is-open':'')+'" type="button" data-action="close-nav" aria-label="'+escapeHtml(copy("f"))+'"'+(!state.navOpen?' aria-hidden="true" tabindex="-1"':'')+'></button>'+'<nav class="catalog-site-nav'+(state.navOpen?' is-open':'')+'" aria-label="Site"'+(!state.navOpen?' aria-hidden="true" inert':'')+'>'+entries.map((entry)=>'<a class="catalog-site-nav__link'+(state.page===entry.id?' is-active':'')+'" href="#'+escapeHtml(pageHash(entry.id))+'" data-action="set-page" data-page-id="'+escapeHtml(entry.id)+'">'+icon(entry.icon)+'<span>'+escapeHtml(pageLabel(entry.id))+'</span>'+'</a>').join("")+'</nav>';}function renderActivePage(){if(state.page==="menu")return renderMenuPage();if(state.page==="about")return renderAboutPage();if(state.page==="contact")return renderContactPage();return renderLandingPage();}function renderFooter(){const name=localText(state.catalog.business.name);const mark=runtimeEntryUrl("creativivid-mark.webp");return'<footer class="catalog-footer"><span>El sitio de '+escapeHtml(name)+' es una creacion de </span><a href="https://creativivid.com/" target="_blank" rel="noopener"><img src="'+escapeHtml(mark)+'" alt="" width="24" height="24"/><strong>Creativivid.com</strong></a></footer>';}function renderModifierGroup(product,ref){const group=groupById(ref.groupId);if(!group)return"";const resolved=applyGroupRef(group,ref);const refKey=modifierRefKey(ref);const type=resolved.maxSelected===1?"radio":"checkbox";const defaults=new Set(defaultOptionIdsForGroup(product,resolved,ref));const dependencyAttrs=ref.dependsOn?.groupId&&Array.isArray(ref.dependsOn.optionIds)&&ref.dependsOn.optionIds.length?' data-dep-group="'+escapeHtml(ref.dependsOn.refId||ref.dependsOn.groupId)+'" data-dep-options="'+escapeHtml(ref.dependsOn.optionIds.join(","))+'"':"";return'<fieldset class="catalog-choice-group" data-choice="'+escapeHtml(refKey)+'" data-group-id="'+escapeHtml(ref.groupId)+'" data-kind="m"'+dependencyAttrs+'>'+'<legend>'+escapeHtml(localText(resolved.label))+(resolved.required?' <span>'+escapeHtml(copy("Q"))+'</span>':'')+'</legend>'+resolved.options.map((option)=>{const inputName="modifier-"+product.id+"-"+refKey;const checked=defaults.has(option.id)?" checked":"";const disabled=option.available?"":" disabled";const priceLabel=typeof option.priceOverride==="number"?money(option.priceOverride):deltaMoney(option.priceDelta);return'<label class="catalog-choice'+(!option.available?' is-disabled':'')+'">'+'<input type="'+type+'" name="'+escapeHtml(inputName)+'" value="'+escapeHtml(option.id)+'"'+checked+disabled+' />'+'<span class="catalog-choice__body"><span>'+escapeHtml(localText(option.label))+'</span>'+renderOptionMeta(option)+'</span>'+(priceLabel?'<strong>'+escapeHtml(priceLabel)+'</strong>':'')+'</label>';}).join("")+'<p class="catalog-choice-group__error" data-choice-error aria-live="polite"></p>'+'</fieldset>';}function renderComboSlot(product,slot){const type=slot.maxSelected===1?"radio":"checkbox";const defaults=new Set(defaultChoiceIds(slot));return'<fieldset class="catalog-choice-group" data-choice="'+escapeHtml(slot.id)+'" data-kind="c">'+'<legend>'+escapeHtml(localText(slot.label))+(slot.required?' <span>'+escapeHtml(copy("Q"))+'</span>':'')+'</legend>'+slot.choices.map((choice)=>{const inputName="combo-"+product.id+"-"+slot.id;const checked=defaults.has(choice.id)?" checked":"";const disabled=choice.available?"":" disabled";return'<label class="catalog-choice'+(!choice.available?' is-disabled':'')+'">'+'<input type="'+type+'" name="'+escapeHtml(inputName)+'" value="'+escapeHtml(choice.id)+'"'+checked+disabled+' />'+'<span>'+escapeHtml(localText(choice.label))+'</span>'+(choice.priceDelta?'<strong>'+escapeHtml(deltaMoney(choice.priceDelta))+'</strong>':'')+'</label>';}).join("")+'<p class="catalog-choice-group__error" data-choice-error aria-live="polite"></p>'+'</fieldset>';}function renderDetail(){if(!state.detailOpen||!state.selectedProductId)return"";const product=productById(state.selectedProductId);if(!product)return"";const name=localText(product.name);const description=localText(product.longDescription)||localText(product.description);const servingSize=localText(product.servingSize);const sectionId=state.selectedProductSectionId||product.sectionIds?.[0]||"";const spriteAttrs=detailSpriteDataAttrs(product,sectionId);return'<section class="catalog-modal" role="dialog" aria-modal="true">'+'<button class="catalog-modal__backdrop" type="button" data-action="close-detail" aria-label="'+escapeHtml(copy("f"))+'"></button>'+'<div class="catalog-modal__panel">'+'<button class="catalog-modal__close catalog-icon-button" type="button" data-action="close-detail" aria-label="'+escapeHtml(copy("f"))+'">'+icon("close")+'</button>'+'<div class="catalog-detail">'+'<div class="catalog-detail__media'+(spriteAttrs?' catalog-detail__media--360':'')+'"'+spriteAttrs+'><img class="catalog-detail__image" src="'+escapeHtml(assetSource(product.media.poster.path,sectionId,product.id,"poster"))+'" alt="" />'+(spriteAttrs?'<canvas class="catalog-detail__interactive-canvas" aria-label="360"></canvas>'+'<span class="catalog-360-badge catalog-360-badge--detail">'+escapeHtml(copy("ab"))+'</span>'+'<span class="catalog-detail__loader" aria-hidden="true"></span>'+'<span class="catalog-detail__motion-cue" aria-hidden="true"><span></span></span>'+'<span class="catalog-detail__fallback-status">'+escapeHtml(copy("U"))+'</span>':'')+'</div>'+'<form class="catalog-detail__content" data-detail-form="'+escapeHtml(product.id)+'">'+'<span class="catalog-detail__badges">'+renderBadges(product)+renderSpice(product)+'</span>'+'<h2>'+escapeHtml(name)+'</h2>'+(servingSize?'<p>'+escapeHtml(servingSize)+'</p>':'')+(description?'<p>'+escapeHtml(description)+'</p>':'')+renderAllergens(product)+'<strong class="catalog-detail__price">'+escapeHtml(productPriceLabel(product,"detail"))+'</strong>'+'<div class="catalog-detail__validation" data-detail-validation role="alert" aria-live="assertive" hidden></div>'+(product.modifierGroupRefs||[]).map((ref)=>renderModifierGroup(product,ref)).join("")+(product.comboSlots||[]).map((slot)=>renderComboSlot(product,slot)).join("")+'<label class="catalog-input">'+escapeHtml(copy("P"))+'<input name="quantity" type="number" min="1" value="1" /></label>'+'<label class="catalog-input">'+escapeHtml(copy("M"))+'<textarea name="note" rows="2"></textarea></label>'+'<button class="catalog-primary catalog-detail__submit" type="submit" '+(!product.available?'disabled':'')+'>'+buttonContent("cart",copy("a"))+'</button>'+'</form></div></div></section>';}function cartCheckoutIssues(){const issues=[];state.cart.items.forEach((selection)=>{const product=productById(selection.productId);if(!product)return;const productIssues=validateProductSelection(product,selection);if(productIssues.length){issues.push({id:product.id,message:copy("k")});}});const mode=activeMode();if(state.cart.fulfillmentModeId&&!mode){issues.push({id:"fulfillmentModeId",message:copy("S")});}if(mode?.requiresTable&&!String(state.cart.customer.table||"").trim()){issues.push({id:"table",message:copy("o")});}if(mode?.requiresAddress&&!String(state.cart.customer.address||"").trim()){issues.push({id:"address",message:copy("n")});}return issues;}function renderCart(){const totals=cartTotals();const mode=activeMode();const checkoutIssues=cartCheckoutIssues();const paymentInstructions=localText(state.catalog.order.paymentInstructions);const tableMissing=Boolean(mode?.requiresTable&&!String(state.cart.customer.table||"").trim());const addressMissing=Boolean(mode?.requiresAddress&&!String(state.cart.customer.address||"").trim());const whatsappDisabled=!totals.lines.length||checkoutIssues.length>0;const clearCartButton='<button class="catalog-cart__clear catalog-icon-button" type="button" data-action="cart-clear" '+(!totals.lines.length?'disabled ':'')+'aria-label="'+escapeHtml(copy("g"))+'">'+icon("trash")+'</button>';return'<aside class="catalog-cart'+(state.cartOpen?' is-open':'')+(!totals.lines.length?' is-empty':'')+'" aria-label="'+escapeHtml(copy("e"))+'" data-cart-count="'+totals.lines.length+'">'+'<div class="catalog-cart__header"><h2>'+escapeHtml(copy("J"))+'</h2><div class="catalog-cart__header-actions">'+clearCartButton+'<button class="catalog-cart__close catalog-icon-button" type="button" data-action="toggle-cart" aria-label="'+escapeHtml(copy("f"))+'">'+icon("close")+'</button></div></div>'+(totals.lines.length?totals.lines.map((line,index)=>'<article class="catalog-cart-line">'+'<div class="catalog-cart-line__summary"><strong>'+escapeHtml(line.quantity+"x "+localText(line.product.name))+'</strong>'+line.modifiers.map((entry)=>'<span>'+escapeHtml(modifierLineText(entry))+'</span>').join("")+line.comboChoices.map((entry)=>'<span>'+escapeHtml(entry.slotLabel+": "+entry.choiceLabel)+'</span>').join("")+(line.note?'<span>'+escapeHtml(copy("H"))+': '+escapeHtml(line.note)+'</span>':'')+'</div>'+'<div class="catalog-cart-line__actions"><strong>'+money(line.lineTotal)+'</strong><span class="catalog-cart-line__quantity-controls"><button class="catalog-icon-button" type="button" data-action="cart-dec" data-line-index="'+index+'" aria-label="'+escapeHtml(copy("m"))+'">'+icon("minus")+'</button><button class="catalog-icon-button" type="button" data-action="cart-inc" data-line-index="'+index+'" aria-label="'+escapeHtml(copy("x"))+'">'+icon("plus")+'</button></span></div>'+'</article>').join(""):'<p class="catalog-empty">'+escapeHtml(copy("F"))+'</p>')+'<label class="catalog-input">'+escapeHtml(copy("E"))+'<input data-cart-field="name" value="'+escapeHtml(state.cart.customer.name||"")+'" /></label>'+'<label class="catalog-input">'+escapeHtml(copy("s"))+'<select data-cart-field="fulfillmentModeId">'+'<option value="">'+escapeHtml(copy("S"))+'</option>'+state.catalog.order.fulfillmentModes.filter((entry)=>entry.enabled).map((entry)=>'<option value="'+escapeHtml(entry.id)+'"'+(state.cart.fulfillmentModeId===entry.id?' selected':'')+'>'+escapeHtml(localText(entry.label))+'</option>').join("")+'</select></label>'+(mode&&mode.requiresTable?'<label class="catalog-input'+(tableMissing?' is-invalid':'')+'">'+escapeHtml(copy("W"))+' <span class="catalog-input__required">'+escapeHtml(copy("Q"))+'</span><input data-cart-field="table" value="'+escapeHtml(state.cart.customer.table||"")+'" required'+(tableMissing?' aria-invalid="true"':'')+' /><span class="catalog-input__error" data-cart-error="table"'+(!tableMissing?' hidden':'')+'>'+escapeHtml(copy("o"))+'</span></label>':'')+(mode&&mode.requiresAddress?'<label class="catalog-input'+(addressMissing?' is-invalid':'')+'">'+escapeHtml(copy("b"))+' <span class="catalog-input__required">'+escapeHtml(copy("Q"))+'</span><input data-cart-field="address" value="'+escapeHtml(state.cart.customer.address||"")+'" required'+(addressMissing?' aria-invalid="true"':'')+' /><span class="catalog-input__error" data-cart-error="address"'+(!addressMissing?' hidden':'')+'>'+escapeHtml(copy("n"))+'</span></label>':'')+'<label class="catalog-input">'+escapeHtml(copy("K"))+'<textarea data-cart-field="note" rows="2">'+escapeHtml(state.cart.note||"")+'</textarea></label>'+'<div class="catalog-cart__totals"><span>'+escapeHtml(copy("V"))+'</span><strong>'+money(totals.subtotal)+'</strong></div>'+totals.fees.map((fee)=>'<div class="catalog-cart__totals"><span>'+escapeHtml(fee.label)+'</span><strong>'+money(fee.amount)+'</strong></div>').join("")+'<div class="catalog-cart__totals catalog-cart__totals--final"><span>'+escapeHtml(copy("X"))+'</span><strong>'+money(totals.total)+'</strong></div>'+(paymentInstructions?'<p class="catalog-cart__payment"><span>'+escapeHtml(paymentInstructions)+'</span></p>':'')+renderDeliveryLinks()+'<p class="catalog-cart__validation" role="alert"'+(!checkoutIssues.length?' hidden':'')+'>'+escapeHtml(checkoutIssues[0]?.message||"")+'</p>'+'<a class="catalog-primary catalog-primary--link catalog-whatsapp-link'+(whatsappDisabled?' is-disabled':'')+'" href="'+escapeHtml(buildWhatsAppUrl())+'"'+(whatsappDisabled?' aria-disabled="true"':'')+' target="_blank" rel="noreferrer">'+buttonContent("whatsapp",copy("T"))+'</a>'+'</aside>';}function modifierLineText(entry){return entry.groupLabel+": "+entry.optionLabel+(typeof entry.variant==="number"?" ("+money(entry.variant)+")":typeof entry.priceOverride==="number"?" ("+money(entry.priceOverride)+")":entry.priceDelta?" ("+money(entry.priceDelta)+")":"");}function orderReference(){return"MM-"+Date.now().toString(36).toUpperCase().slice(-6);}function buildWhatsAppMessage(){const totals=cartTotals();const contactMode=activeMode();const paymentInstructions=localText(state.catalog.order.paymentInstructions);const lines=["*"+copy("J")+" "+orderReference()+"*",localText(state.catalog.business.name),"",state.cart.customer.name?copy("l")+": "+state.cart.customer.name:"",contactMode?copy("D")+": "+localText(contactMode.label):"",state.cart.customer.table?copy("W")+": "+state.cart.customer.table:"",state.cart.customer.address?copy("b")+": "+state.cart.customer.address:"","",...totals.lines.flatMap((line,index)=>[(index+1)+". "+line.quantity+"x "+localText(line.product.name)+" - "+money(line.lineTotal),...line.modifiers.map((entry)=>"   - "+modifierLineText(entry)),...line.comboChoices.map((entry)=>"   - "+entry.slotLabel+": "+entry.choiceLabel),line.note?"   "+copy("H")+": "+line.note:""]),"",copy("V")+": "+money(totals.subtotal),...totals.fees.map((fee)=>copy("p")+": "+fee.label+" - "+money(fee.amount)),copy("X")+": "+money(totals.total),paymentInstructions||"",state.cart.note?copy("K")+": "+state.cart.note:""].filter(Boolean);return lines.join("\n");}function buildWhatsAppUrl(){const contactId=state.catalog.order.whatsapp.defaultContactId;const contact=state.catalog.order.whatsapp.contacts.find((entry)=>entry.id===contactId);const phone=contact?String(contact.phone||"").replace(/[^\d]/g,""):"";if(!phone||!state.cart.items.length||cartCheckoutIssues().length)return"#";return"https://wa.me/"+phone+"?text="+encodeURIComponent(buildWhatsAppMessage());}function syncConditionalGroups(form){const selected=new Map();form.querySelectorAll('.catalog-choice-group[data-kind="m"]').forEach((group)=>{const depGroup=group.getAttribute("data-dep-group")||"";const depOptions=(group.getAttribute("data-dep-options")||"").split(",").filter(Boolean);const applies=!depGroup||!depOptions.length||depOptions.some((optionId)=>selected.get(depGroup)?.has(optionId));group.hidden=!applies;group.querySelectorAll("input").forEach((input)=>{input.disabled=!applies||Boolean(input.closest(".is-disabled"));if(!applies)input.checked=false;});if(!applies){clearChoiceGroupError(group);return;}const groupId=group.getAttribute("data-choice")||"";const checked=[...group.querySelectorAll("input:checked")].map((input)=>input.value);if(groupId&&checked.length)selected.set(groupId,new Set(checked));});}function syncCartCheckoutState(){const cart=app.querySelector(".catalog-cart");if(!cart)return;const issues=cartCheckoutIssues();const validation=cart.querySelector(".catalog-cart__validation");if(validation){validation.textContent=issues[0]?.message||"";validation.hidden=!issues.length;}["table","address"].forEach((field)=>{const input=cart.querySelector('[data-cart-field="'+field+'"]');if(!input)return;const missing=issues.some((issue)=>issue.id===field);const label=input.closest(".catalog-input");const error=cart.querySelector('[data-cart-error="'+field+'"]');label?.classList.toggle("is-invalid",missing);if(missing)input.setAttribute("aria-invalid","true");else input.removeAttribute("aria-invalid");if(error){error.hidden=!missing;error.textContent=missing?(field==="table"?copy("o"):copy("n")):"";}});const link=cart.querySelector(".catalog-primary--link");if(!link)return;const disabled=!state.cart.items.length||issues.length>0;link.classList.toggle("is-disabled",disabled);if(disabled)link.setAttribute("aria-disabled","true");else link.removeAttribute("aria-disabled");link.setAttribute("href",disabled?"#":buildWhatsAppUrl());}function readDetailSelection(form){syncConditionalGroups(form);const productId=form.getAttribute("data-detail-form");const product=productById(productId);const modifierSelections=(product.modifierGroupRefs||[]).flatMap((ref)=>{const refKey=modifierRefKey(ref);const group=form.querySelector('.catalog-choice-group[data-kind="m"][data-choice="'+CSS.escape(refKey)+'"]');if(group?.hidden)return[];const inputs=group?[...group.querySelectorAll("input:checked")]:[];return[{groupId:ref.groupId,...(ref.refId?{refId:ref.refId}:{}),optionIds:inputs.map((input)=>input.value)}];});const comboSelections=(product.comboSlots||[]).map((slot)=>{const group=form.querySelector('.catalog-choice-group[data-kind="c"][data-choice="'+CSS.escape(slot.id)+'"]');const inputs=group?[...group.querySelectorAll("input:checked")]:[];return{slotId:slot.id,choiceIds:inputs.map((input)=>input.value)};});return{productId,quantity:Math.max(1,Number(form.elements.quantity.value||1)),modifierSelections,comboSelections,note:form.elements.note.value.trim()};}function clearChoiceGroupError(group){if(!group)return;group.classList.remove("is-invalid");group.removeAttribute("aria-invalid");const error=group.querySelector("[data-choice-error]");if(error)error.textContent="";const form=group.closest("[data-detail-form]");if(!form||form.querySelector(".catalog-choice-group.is-invalid"))return;const summary=form.querySelector("[data-detail-validation]");if(summary){summary.textContent="";summary.hidden=true;}}function clearDetailValidation(form){const summary=form.querySelector("[data-detail-validation]");if(summary){summary.textContent="";summary.hidden=true;}form.querySelectorAll(".catalog-choice-group.is-invalid").forEach(clearChoiceGroupError);}function detailChoiceGroupForIssue(form,issue){if(issue.kind!=="modifier"&&issue.kind!=="combo")return null;return form.querySelector('[data-kind="'+(issue.kind==="modifier"?"m":"c")+'"][data-choice="'+CSS.escape(issue.id)+'"]');}function showDetailValidation(form,issues){clearDetailValidation(form);const relevant=issues.filter((issue)=>issue.kind==="modifier"||issue.kind==="combo");const summary=form.querySelector("[data-detail-validation]");if(summary){summary.textContent=copy("k");summary.hidden=false;}relevant.forEach((issue)=>{const group=detailChoiceGroupForIssue(form,issue);if(!group)return;group.classList.add("is-invalid");group.setAttribute("aria-invalid","true");const error=group.querySelector("[data-choice-error]");if(error)error.textContent=issue.message;});const firstInvalid=form.querySelector(".catalog-choice-group.is-invalid");if(firstInvalid){firstInvalid.scrollIntoView({block:"center",behavior:shouldReduceMotion()?"auto":"smooth"});const firstInput=firstInvalid.querySelector("input:not(:disabled)");if(firstInput&&typeof firstInput.focus==="function"){window.setTimeout(()=>firstInput.focus({preventScroll:true}),80);}}}function hydrateMedia(){const sections=[...app.querySelectorAll("[data-carousel-section]")];const connectionPolicy=readConnectionPolicy();if(!("IntersectionObserver"in window)){const firstSectionId=sections[0]?.getAttribute("data-section-id");if(firstSectionId)visibleCarouselSectionIds.add(firstSectionId);if(sections[0])ensureCarouselInstance(sections[0]);sections.slice(0,1).forEach(markCarouselVisible);queueFocusedCarouselUpdate();return;}const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{const sectionId=entry.target.getAttribute("data-section-id")||"";if(!sectionId)return;if(entry.isIntersecting&&entry.intersectionRatio>0.08){ensureCarouselInstance(entry.target);visibleCarouselSectionIds.add(sectionId);markCarouselVisible(entry.target);}else{visibleCarouselSectionIds.delete(sectionId);}});queueFocusedCarouselUpdate();},{rootMargin:connectionPolicy.profile==="desktop"?"80px 0px":"24px 0px",threshold:[0,0.08,0.24,0.5]});sections.forEach((section)=>observer.observe(section));const refreshFocus=()=>queueFocusedCarouselUpdate();window.addEventListener("scroll",refreshFocus,{passive:true});window.addEventListener("resize",refreshFocus,{passive:true});}let backgroundWarmupScheduled=false;function prefetchProductDetailAssets(productId,sectionId){const product=productById(productId);if(!product)return;const connectionPolicy=readConnectionPolicy();if(!connectionPolicy.allowIntentPrefetch)return;const detailSprite=product.media.sprite360;if(!detailSprite?.path)return;const detailSrc=assetSource(detailSprite.path,sectionId,productId,"sprite360");void loadSprite360Image(detailSrc,{bucket:"detail",bytes:detailSprite.bytes}).catch(()=>undefined);}function scheduleBackgroundWarmup(){const connectionPolicy=readConnectionPolicy();if(!hasUserInteraction||!connectionPolicy.allowBackgroundWarmup||backgroundWarmupScheduled)return;backgroundWarmupScheduled=true;const runner=()=>{backgroundWarmupScheduled=false;const focusedEntry=focusedCarouselSectionId?carouselInstances.get(focusedCarouselSectionId):null;const firstVisible=focusedEntry||Array.from(carouselInstances.values()).find((entry)=>entry.visible)||carouselInstances.values().next().value;if(!firstVisible)return;const slide=firstVisible.embla.slideNodes()[carouselActiveIndex(firstVisible)];const button=slide?.querySelector('[data-action="open-product"]');const productId=button?.getAttribute("data-product-id")||"";const sectionId=button?.getAttribute("data-product-section-id")||"";if(!productId)return;prefetchProductDetailAssets(productId,sectionId);};if(typeof window.requestIdleCallback==="function"){window.requestIdleCallback(runner,{timeout:4000});return;}window.setTimeout(runner,4000);}function setupInteractiveDetailMedia(){const host=app.querySelector(".catalog-detail__media[data-sprite360-src]");if(!host||host.dataset.interactiveReady)return;const canvas=host.querySelector(".catalog-detail__interactive-canvas");const fallbackImage=host.querySelector(".catalog-detail__image");const src=host.getAttribute("data-sprite360-src")||"";const initialRole=host.getAttribute("data-sprite360-role")||"sprite360";const frameCountRaw=Number(host.getAttribute("data-sprite360-frame-count")||0);const columnsRaw=Number(host.getAttribute("data-sprite360-columns")||0);const rowsRaw=Number(host.getAttribute("data-sprite360-rows")||0);const frameWidth=Math.max(1,Number(host.getAttribute("data-sprite360-frame-width")||0));const frameHeight=Math.max(1,Number(host.getAttribute("data-sprite360-frame-height")||0));const fps=Math.max(1,Number(host.getAttribute("data-sprite360-fps")||12));const spriteBytes=Math.max(0,Number(host.getAttribute("data-sprite360-bytes")||0));const columns=Math.max(1,Math.round(columnsRaw));const rows=Math.max(1,Math.round(rowsRaw));const frameCount=Math.max(2,Math.min(Math.round(frameCountRaw),columns*rows));const rotateDirection=host.getAttribute("data-sprite360-rotation-direction")==="ccw"?1:-1;const hqSrc=host.getAttribute("data-detail-hq-src")||"";const hqColumnsRaw=Number(host.getAttribute("data-detail-hq-columns")||0);const hqRowsRaw=Number(host.getAttribute("data-detail-hq-rows")||0);const hqColumns=Math.max(1,Math.round(hqColumnsRaw));const hqRows=Math.max(1,Math.round(hqRowsRaw));const hqMeta=hqSrc?{frameCount:Math.max(2,Math.min(Math.round(Number(host.getAttribute("data-detail-hq-frame-count")||0)),hqColumns*hqRows)),columns:hqColumns,rows:hqRows,frameWidth:Math.max(1,Number(host.getAttribute("data-detail-hq-frame-width")||0)),frameHeight:Math.max(1,Number(host.getAttribute("data-detail-hq-frame-height")||0)),fps:Math.max(1,Number(host.getAttribute("data-detail-hq-fps")||12)),bytes:Math.max(0,Number(host.getAttribute("data-detail-hq-bytes")||0))}:null;if(!canvas||!src||!frameCount||!columns||!rows)return;const initialBucket=initialRole==="listSprite360"?"list":"detail";const initialReady=isSprite360ImageReady(src,initialBucket);const motionCueKey=hqSrc||src;host.dataset.interactiveReady=initialReady?"restoring":"loading";host.classList.toggle("is-loading-interactive",!initialReady);if(detailMotionCueSeenKeys.has(motionCueKey)){host.classList.add("has-seen-interactive-cue");}const initialMeta={frameCount,columns,rows,frameWidth,frameHeight,fps};const startInteractiveLoad=()=>{loadSprite360Image(src,{bucket:initialRole==="listSprite360"?"list":"detail",bytes:spriteBytes}).then((image)=>{if(!host.isConnected)return;const context=canvas.getContext("2d",{alpha:true,willReadFrequently:true});if(!context)throw new Error("sprite360 canvas unavailable");let frameCursor=0;let activePointerId=null;let gestureMode="idle";let lastAngleRad=null;let animationId=0;let lastAnimationMs=0;let settleAnimationId=0;let zoomScale=1;let zoomTranslateX=0;let zoomTranslateY=0;let pinchStartDistance=1;let pinchStartScale=1;let pinchStartCenter={x:0,y:0};let pinchStartTranslateX=0;let pinchStartTranslateY=0;const activePointers=new Map();let activeImage=image;let atlasFrame=initialMeta;let visibleFrameIndexes=[];const renderFrame=()=>{const rendered=renderVisibleSpriteFrame(context,activeImage,atlasFrame,visibleFrameIndexes,frameCursor);if(rendered){host.dataset.interactiveFrame=String(resolveVisibleSpriteFrameIndex(visibleFrameIndexes,frameCursor));}return rendered;};const applySpriteImage=(nextImage,nextSrc,nextMeta,tier)=>{const resolvedPlayback=resolveSpritePlaybackFrames(nextSrc,nextImage,nextMeta,context);activeImage=nextImage;atlasFrame=resolvedPlayback.frame;visibleFrameIndexes=resolvedPlayback.visibleFrameIndexes;canvas.width=atlasFrame.frameWidth;canvas.height=atlasFrame.frameHeight;host.dataset.interactiveTier=tier;return renderFrame();};const stopAutoplay=()=>{if(!animationId)return;cancelAnimationFrame(animationId);animationId=0;};const clamp=(value,min,max)=>Math.min(max,Math.max(min,value));const pointerFromEvent=(event)=>({x:event.clientX,y:event.clientY});const activePointerList=()=>[...activePointers.values()];const pointerCenter=(pointers)=>{const total=pointers.reduce((sum,pointer)=>({x:sum.x+pointer.x,y:sum.y+pointer.y}),{x:0,y:0});return{x:total.x/Math.max(1,pointers.length),y:total.y/Math.max(1,pointers.length)};};const pointerDistance=(pointers)=>{if(pointers.length<2)return 1;return Math.max(1,Math.hypot(pointers[1].x-pointers[0].x,pointers[1].y-pointers[0].y));};const clearSettleAnimation=()=>{if(!settleAnimationId)return;cancelAnimationFrame(settleAnimationId);settleAnimationId=0;};const applyZoomTransform=()=>{const rect=host.getBoundingClientRect();const maxX=Math.max(0,(rect.width*(zoomScale-1))/2);const maxY=Math.max(0,(rect.height*(zoomScale-1))/2);zoomTranslateX=clamp(zoomTranslateX,-maxX,maxX);zoomTranslateY=clamp(zoomTranslateY,-maxY,maxY);canvas.style.transform="translate("+zoomTranslateX.toFixed(2)+"px, "+zoomTranslateY.toFixed(2)+"px) scale("+zoomScale.toFixed(3)+")";host.dataset.zoomScale=zoomScale.toFixed(2);host.classList.toggle("is-zoomed-interactive",zoomScale>1.01);};const autoplay=(now)=>{if(!host.isConnected||host.classList.contains("is-dragging-interactive")||host.classList.contains("is-pinching-interactive")||settleAnimationId){animationId=0;return;}if(!lastAnimationMs||now-lastAnimationMs>=1000/atlasFrame.fps){frameCursor+=1;renderFrame();lastAnimationMs=now;}animationId=requestAnimationFrame(autoplay);};const startAutoplay=()=>{if(animationId||settleAnimationId||!host.isConnected||window.matchMedia("(prefers-reduced-motion: reduce)").matches||host.classList.contains("is-dragging-interactive")||host.classList.contains("is-pinching-interactive")||activePointers.size>0){return;}lastAnimationMs=0;animationId=requestAnimationFrame(autoplay);};const settleZoomToBase=()=>{clearSettleAnimation();const startScale=zoomScale;const startTranslateX=zoomTranslateX;const startTranslateY=zoomTranslateY;if(startScale<=1.01&&Math.abs(startTranslateX)<=0.5&&Math.abs(startTranslateY)<=0.5){zoomScale=1;zoomTranslateX=0;zoomTranslateY=0;applyZoomTransform();startAutoplay();return;}const durationMs=window.matchMedia("(prefers-reduced-motion: reduce)").matches?0:160;if(!durationMs){zoomScale=1;zoomTranslateX=0;zoomTranslateY=0;applyZoomTransform();startAutoplay();return;}const startMs=performance.now();const step=(now)=>{if(!host.isConnected){settleAnimationId=0;return;}const progress=Math.min(1,(now-startMs)/durationMs);const eased=1-Math.pow(1-progress,3);zoomScale=startScale+(1-startScale)*eased;zoomTranslateX=startTranslateX*(1-eased);zoomTranslateY=startTranslateY*(1-eased);applyZoomTransform();if(progress>=1){settleAnimationId=0;zoomScale=1;zoomTranslateX=0;zoomTranslateY=0;applyZoomTransform();startAutoplay();return;}settleAnimationId=requestAnimationFrame(step);};settleAnimationId=requestAnimationFrame(step);};const beginPinchGesture=()=>{const pointers=activePointerList();if(pointers.length<2)return;gestureMode="pinch";activePointerId=null;lastAngleRad=null;pinchStartDistance=pointerDistance(pointers);pinchStartScale=zoomScale;pinchStartCenter=pointerCenter(pointers);pinchStartTranslateX=zoomTranslateX;pinchStartTranslateY=zoomTranslateY;host.classList.remove("is-dragging-interactive");host.classList.add("is-pinching-interactive");};const updatePinchGesture=()=>{const pointers=activePointerList();if(pointers.length<2)return;const center=pointerCenter(pointers);zoomScale=clamp(pinchStartScale*(pointerDistance(pointers)/pinchStartDistance),1,2.6);zoomTranslateX=pinchStartTranslateX+center.x-pinchStartCenter.x;zoomTranslateY=pinchStartTranslateY+center.y-pinchStartCenter.y;applyZoomTransform();};applyZoomTransform();const sampleFromEvent=(event)=>readInteractiveOrbitSample(event.clientX,event.clientY,resolveInteractiveOrbitFrame(host.getBoundingClientRect()));const onPointerDown=(event)=>{clearSettleAnimation();host.classList.add("has-used-interactive");detailMotionCueSeenKeys.add(motionCueKey);activePointers.set(event.pointerId,pointerFromEvent(event));canvas.setPointerCapture?.(event.pointerId);stopAutoplay();if(activePointers.size>=2){beginPinchGesture();event.preventDefault();return;}const sample=sampleFromEvent(event);if(sample.kind!=="sample")return;gestureMode="rotate";activePointerId=event.pointerId;lastAngleRad=sample.angleRad;host.classList.add("is-dragging-interactive");event.preventDefault();};const onPointerMove=(event)=>{if(activePointers.has(event.pointerId)){activePointers.set(event.pointerId,pointerFromEvent(event));}if(gestureMode==="pinch"||activePointers.size>=2){updatePinchGesture();event.preventDefault();return;}if(activePointerId!==event.pointerId)return;const orbitFrame=resolveInteractiveOrbitFrame(host.getBoundingClientRect());const sample=readInteractiveOrbitSample(event.clientX,event.clientY,orbitFrame);if(sample.kind!=="sample"){lastAngleRad=null;return;}if(lastAngleRad===null){lastAngleRad=sample.angleRad;return;}const deltaPx=resolveInteractiveVirtualDeltaPx(lastAngleRad,sample.angleRad,orbitFrame);lastAngleRad=sample.angleRad;frameCursor+=(deltaPx/resolveInteractivePixelsPerFrame(orbitFrame,visibleFrameIndexes.length))*rotateDirection;renderFrame();event.preventDefault();};const onPointerRelease=(event)=>{activePointers.delete(event.pointerId);const shouldSettleZoom=activePointers.size<2&&zoomScale>1.01;const endedPinch=gestureMode==="pinch"&&activePointers.size<2;const shouldResumeAutoplay=activePointers.size===0&&!shouldSettleZoom;if(endedPinch||shouldSettleZoom){gestureMode="idle";host.classList.remove("is-pinching-interactive");if(shouldSettleZoom)settleZoomToBase();}if(activePointerId!==event.pointerId&&activePointers.size>0){canvas.releasePointerCapture?.(event.pointerId);return;}activePointerId=null;lastAngleRad=null;if(activePointers.size===0){gestureMode="idle";}host.classList.remove("is-dragging-interactive");canvas.releasePointerCapture?.(event.pointerId);if(shouldResumeAutoplay){startAutoplay();}};if(!applySpriteImage(image,src,initialMeta,hqMeta?"preview":"detail")){stopAutoplay();host.dataset.interactiveReady="failed";host.classList.remove("is-loading-interactive","is-interactive-ready");host.classList.add("is-interactive-failed");return;}startAutoplay();canvas.addEventListener("pointerdown",onPointerDown);canvas.addEventListener("pointermove",onPointerMove);canvas.addEventListener("pointerup",onPointerRelease);canvas.addEventListener("pointercancel",onPointerRelease);canvas.addEventListener("dragstart",(event)=>event.preventDefault());canvas.addEventListener("pointerleave",()=>{lastAngleRad=null;});host.dataset.interactiveReady="ready";host.classList.remove("is-loading-interactive");host.classList.add("is-interactive-ready");if(detailMotionCueSeenKeys.has(motionCueKey)){host.classList.add("has-seen-interactive-cue");}fallbackImage?.setAttribute("aria-hidden","true");if(hqMeta){host.classList.toggle("is-loading-hq",!isSprite360ImageReady(hqSrc,"detail"));loadSprite360Image(hqSrc,{bucket:"detail",bytes:hqMeta.bytes}).then((hqImage)=>{const promote=()=>{if(!host.isConnected||host.dataset.interactiveReady!=="ready")return;if(activePointers.size>0||host.classList.contains("is-dragging-interactive")||host.classList.contains("is-pinching-interactive")){window.setTimeout(promote,140);return;}stopAutoplay();if(applySpriteImage(hqImage,hqSrc,hqMeta,"detail")){host.classList.remove("is-loading-hq");startAutoplay();}};promote();}).catch(()=>{host.classList.remove("is-loading-hq");});}}).catch(()=>{host.dataset.interactiveReady="failed";host.classList.remove("is-loading-interactive","is-loading-hq");host.classList.add("is-interactive-failed");});};if(window.matchMedia("(pointer: coarse)").matches){if(typeof window.requestAnimationFrame==="function"){window.requestAnimationFrame(()=>{window.setTimeout(startInteractiveLoad,0);});}else{window.setTimeout(startInteractiveLoad,32);}return;}startInteractiveLoad();}function render(){destroyCarousels();if(!state.catalog){app.innerHTML='<p class="catalog-loading">'+escapeHtml(copy("A"))+'</p>';return;}const businessName=localText(state.catalog.business.name);const logoPath=state.catalog.business?.logo?.path||"";const logoHtml=logoPath?'<img class="catalog-header__logo" src="'+escapeHtml(assetSource(logoPath))+'" alt="'+escapeHtml(businessName)+'" />':'<span class="catalog-header__logo catalog-header__logo--fallback">'+icon("foodMenu")+'</span>';app.innerHTML='<div class="catalog-shell" data-active-page="'+escapeHtml(state.page)+'">'+'<header class="catalog-header"><button class="catalog-header__menu catalog-icon-button" type="button" data-action="toggle-nav" aria-label="'+escapeHtml(copy("N"))+'" aria-expanded="'+(state.navOpen?"true":"false")+'">'+icon("menu")+'</button>'+'<button class="catalog-header__brand" type="button" data-action="set-page" data-page-id="home" aria-label="'+escapeHtml(businessName)+'">'+logoHtml+'</button>'+'<div class="catalog-header__actions"><select data-action="set-locale">'+state.catalog.meta.locales.map((locale)=>'<option value="'+escapeHtml(locale)+'"'+(state.locale===locale?' selected':'')+'>'+escapeHtml(locale.toUpperCase())+'</option>').join("")+'</select></div></header>'+renderSiteNav()+(state.page==="menu"?'<button class="catalog-header__cart" type="button" data-action="toggle-cart" data-cart-count="'+escapeHtml(state.cart.items.length)+'" aria-label="'+escapeHtml(copy("e")+' ('+state.cart.items.length+')')+'">'+buttonContent("cart",copy("e")+' ('+state.cart.items.length+')')+'</button>':'')+renderActivePage()+renderFooter()+renderCart()+renderDetail()+'</div>';app.dataset.connectionProfile=readConnectionPolicy().profile;app.dataset.runtimeManifest=state.runtimeManifest?"ready":"missing";initCarousels();hydrateMedia();setupInteractiveDetailMedia();app.querySelectorAll("[data-detail-form]").forEach(syncConditionalGroups);scheduleBackgroundWarmup();syncHeaderVisibility();if(pendingScrollRestoreY!==null){const restoreY=Math.max(0,Number(pendingScrollRestoreY||0));pendingScrollRestoreY=null;const restore=()=>window.scrollTo({top:restoreY,behavior:"auto"});if(typeof window.requestAnimationFrame==="function"){window.requestAnimationFrame(()=>window.requestAnimationFrame(restore));}else{window.setTimeout(restore,0);}}document.documentElement.setAttribute("data-runtime-boot","ready");if(window.parent&&window.parent!==window){window.parent.postMessage({type:"menumaker-catalog2-preview-ready",page:state.page},"*");}}function usesMobileHeaderCollapse(){return window.matchMedia("(max-width: 799px)").matches||window.matchMedia("(max-height: 520px) and (hover: none) and (pointer: coarse)").matches;}function syncHeaderVisibility(){if(!app)return;const mobile=usesMobileHeaderCollapse();if(!mobile||state.cartOpen||state.detailOpen||window.scrollY<72){headerHidden=false;app.classList.remove("is-header-hidden");return;}app.classList.toggle("is-header-hidden",headerHidden);}function arrowKeyTargetEntry(){if(focusedCarouselSectionId&&carouselInstances.has(focusedCarouselSectionId)){return carouselInstances.get(focusedCarouselSectionId);}return Array.from(carouselInstances.values()).find((entry)=>entry.visible)||carouselInstances.values().next().value||null;}function scrollMenuSectionIntoView(sectionElement){if(!sectionElement)return;const currentY=window.scrollY;const sectionTop=sectionElement.getBoundingClientRect().top;if(usesMobileHeaderCollapse()&&!state.cartOpen&&!state.detailOpen){headerHidden=sectionTop>0&&currentY+sectionTop>72;app.classList.toggle("is-header-hidden",headerHidden);}const nav=app.querySelector(".catalog-category-nav");const navBottom=nav?parseInt(getComputedStyle(nav).top)+nav.offsetHeight:0;const anchor=sectionElement.querySelector("h2")||sectionElement;const top=Math.max(0,currentY+anchor.getBoundingClientRect().top-navBottom-16);lastScrollY=top;window.scrollTo(0,top);}function moveSectionFocus(direction){const sections=Array.from(app.querySelectorAll("[data-carousel-section]"));if(!sections.length)return false;const currentId=arrowKeyTargetEntry()?.sectionId||focusedCarouselSectionId||sections[0].getAttribute("data-section-id")||"";const currentIndex=Math.max(0,sections.findIndex((section)=>section.getAttribute("data-section-id")===currentId));const nextIndex=Math.min(sections.length-1,Math.max(0,currentIndex+direction));const nextSection=sections[nextIndex];if(!nextSection||nextIndex===currentIndex)return false;focusedCarouselSectionId=nextSection.getAttribute("data-section-id")||"";ensureCarouselInstance(nextSection);scrollMenuSectionIntoView(nextSection);queueFocusedCarouselUpdate();return true;}function handleMenuArrowKey(event){if(state.page!=="menu"||state.detailOpen||state.cartOpen||window.matchMedia("(hover: none)").matches)return;const target=event.target;const tag=String(target?.tagName||"").toLowerCase();if(tag==="input"||tag==="textarea"||tag==="select"||target?.isContentEditable)return;if(!["ArrowLeft","ArrowRight","ArrowUp","ArrowDown"].includes(event.key))return;const entry=arrowKeyTargetEntry();if((event.key==="ArrowLeft"||event.key==="ArrowRight")&&entry){event.preventDefault();event.key==="ArrowLeft"?entry.embla.scrollPrev():entry.embla.scrollNext();return;}if(event.key==="ArrowUp"||event.key==="ArrowDown"){if(moveSectionFocus(event.key==="ArrowUp"?-1:1))event.preventDefault();}}function handleHeaderScroll(){if(headerScrollTicking)return;headerScrollTicking=true;window.requestAnimationFrame(()=>{const currentY=window.scrollY;const mobile=usesMobileHeaderCollapse();if(!mobile||state.cartOpen||state.detailOpen||currentY<72){headerHidden=false;}else if(currentY>lastScrollY+6){headerHidden=true;}else if(currentY<lastScrollY-10){headerHidden=false;}app.classList.toggle("is-header-hidden",headerHidden);lastScrollY=Math.max(0,currentY);headerScrollTicking=false;});}app.addEventListener("input",(event)=>{const target=event.target;const field=target.getAttribute?.("data-cart-field");if(!field)return;if(field==="fulfillmentModeId")state.cart.fulfillmentModeId=target.value;if(field==="name")state.cart.customer.name=target.value;if(field==="table")state.cart.customer.table=target.value;if(field==="address")state.cart.customer.address=target.value;if(field==="note")state.cart.note=target.value;saveCart();if(field==="fulfillmentModeId")render();else syncCartCheckoutState();});app.addEventListener("change",(event)=>{const target=event.target;if(target.getAttribute?.("data-action")==="set-locale"){state.locale=target.value;render();return;}const choiceGroup=target.closest?.(".catalog-choice-group");if(choiceGroup){clearChoiceGroupError(choiceGroup);const form=choiceGroup.closest("[data-detail-form]");if(form)syncConditionalGroups(form);}});app.addEventListener("submit",(event)=>{const form=event.target;if(!form.matches("[data-detail-form]"))return;event.preventDefault();const selection=readDetailSelection(form);const product=productById(selection.productId);if(!product)return;const issues=validateProductSelection(product,selection);if(issues.length){showDetailValidation(form,issues);return;}addCartSelection(selection);state.detailOpen=false;state.cartOpen=true;render();});app.addEventListener("pointerenter",(event)=>{const card=event.target?.closest?.("[data-product-card]");if(card&&useHoverListSpriteMode()&&!state.detailOpen){setActiveListSprite(card.querySelector("[data-sprite360-src]"));}const target=event.target?.closest?.('[data-action="open-product"]');if(!target)return;prefetchProductDetailAssets(target.getAttribute("data-product-id")||"",target.getAttribute("data-product-section-id")||"");},true);app.addEventListener("pointerleave",(event)=>{const card=event.target?.closest?.("[data-product-card]");if(!card||!useHoverListSpriteMode())return;if(activeListSpriteController?.media&&card.contains(activeListSpriteController.media)){setActiveListSprite(null);}},true);app.addEventListener("touchstart",(event)=>{hasUserInteraction=true;scheduleBackgroundWarmup();},{passive:true,capture:true});app.addEventListener("toggle",(event)=>{if(syncingFaqAccordion)return;const detail=event.target;if(!detail?.matches?.("[data-faq]"))return;const group=Array.from(app.querySelectorAll("[data-faq]"));syncingFaqAccordion=true;if(detail.open){group.forEach((entry)=>{if(entry!==detail)entry.open=false;});}else if(!group.some((entry)=>entry.open)){detail.open=true;}syncingFaqAccordion=false;},true);app.addEventListener("click",(event)=>{hasUserInteraction=true;scheduleBackgroundWarmup();const target=event.target.closest("[data-action]");if(!target)return;const action=target.getAttribute("data-action");if(action==="toggle-nav"){state.navOpen=!state.navOpen;render();return;}if(action==="close-nav"){state.navOpen=false;render();return;}if(action==="set-page"){const page=target.getAttribute("data-page-id")||"home";event.preventDefault();setPage(page);return;}if(action==="scroll-target"){const id=target.getAttribute("data-scroll-target-id");const node=id?document.getElementById(id):null;if(!node)return;event.preventDefault();const sectionId=node.getAttribute("data-section-id")||"";if(sectionId){focusedCarouselSectionId=sectionId;syncCategoryNavActive(sectionId);}scrollMenuSectionIntoView(node);if(window.history?.pushState)window.history.pushState(null,"","#"+id);return;}if(action==="toggle-filter"){const id=target.getAttribute("data-filter-id");if(!id)return;if(state.filters.has(id))state.filters.delete(id);else state.filters.add(id);render();}if(action==="toggle-allergen-filter"){const id=target.getAttribute("data-allergen-filter-id");if(!id)return;if(state.allergenFilters.has(id))state.allergenFilters.delete(id);else state.allergenFilters.add(id);render();}if(action==="add-default-product"){const product=productById(target.getAttribute("data-product-id"));if(!product||!product.available)return;const selection=defaultProductSelection(product);const issues=validateProductSelection(product,selection);if(issues.length){detailReturnScrollY=window.scrollY;state.selectedProductId=product.id;state.selectedProductSectionId=target.getAttribute("data-product-section-id")||product.sectionIds?.[0]||"";state.detailOpen=true;render();window.setTimeout(()=>{const form=app.querySelector('[data-detail-form="'+CSS.escape(product.id)+'"]');if(form)showDetailValidation(form,issues);},0);return;}addCartSelection(selection);state.cartOpen=true;render();return;}if(action==="open-product"){const product=productById(target.getAttribute("data-product-id"));if(!product||!product.available)return;detailReturnScrollY=window.scrollY;state.selectedProductId=product.id;state.selectedProductSectionId=target.getAttribute("data-product-section-id")||product.sectionIds?.[0]||"";state.detailOpen=true;render();}if(action==="close-detail"){pendingScrollRestoreY=detailReturnScrollY;state.detailOpen=false;render();}if(action==="toggle-cart"){state.cartOpen=!state.cartOpen;render();}if(action==="cart-clear"){clearCart();render();}if(action==="carousel-prev"||action==="carousel-next"){const entry=carouselInstances.get(target.getAttribute("data-section-id"));if(!entry)return;focusedCarouselSectionId=entry.sectionId;focusedCarouselSectionPinnedUntil=Date.now()+1200;focusedCarouselSectionPinnedScrollY=window.scrollY;syncCategoryNavActive(entry.sectionId);if(action==="carousel-prev")entry.embla.scrollPrev();if(action==="carousel-next")entry.embla.scrollNext();queueFocusedCarouselUpdate();}if(action==="carousel-thumb"){const entry=carouselInstances.get(target.getAttribute("data-section-id"));const index=Number(target.getAttribute("data-slide-index"));if(!entry||Number.isNaN(index))return;entry.embla.scrollTo(index);focusedCarouselSectionId=entry.sectionId;focusedCarouselSectionPinnedUntil=Date.now()+1200;focusedCarouselSectionPinnedScrollY=window.scrollY;carouselSelectedIndexBySection.set(entry.sectionId,index);queueFocusedCarouselUpdate();}if(action==="cart-inc"||action==="cart-dec"){const index=Number(target.getAttribute("data-line-index"));const current=state.cart.items[index];if(!current)return;const quantity=action==="cart-inc"?current.quantity+1:current.quantity-1;if(quantity<=0)state.cart.items.splice(index,1);else state.cart.items[index]={...current,quantity};saveCart();render();}});window.addEventListener("scroll",handleHeaderScroll,{passive:true});window.addEventListener("resize",syncHeaderVisibility);window.addEventListener("scroll",()=>{if(hasUserInteraction)return;hasUserInteraction=true;scheduleBackgroundWarmup();},{passive:true,once:true});window.addEventListener("keydown",(event)=>{if(hasUserInteraction)return;hasUserInteraction=true;scheduleBackgroundWarmup();},{once:true});window.addEventListener("keydown",handleMenuArrowKey);window.addEventListener("hashchange",()=>{const nextPage=pageFromHash();if(nextPage===state.page)return;state.page=nextPage;state.navOpen=false;if(nextPage!=="menu")state.cartOpen=false;render();});const bootCatalog=window.__MENUMAKER_CATALOG__?Promise.resolve(window.__MENUMAKER_CATALOG__):fetch("./catalog.json",{cache:"no-cache"}).then((response)=>{if(!response.ok)throw new Error("Catalog request failed");return response.json();});const bootRuntimeManifest=Object.prototype.hasOwnProperty.call(window,"__MENUMAKER_RUNTIME_MANIFEST__")?Promise.resolve(window.__MENUMAKER_RUNTIME_MANIFEST__):fetch("./runtime-manifest.json",{cache:"no-cache"}).then((response)=>(response.ok?response.json():null)).catch(()=>null);Promise.all([bootCatalog,bootRuntimeManifest]).then(([catalog,runtimeManifest])=>{registerRuntimeManifest(runtimeManifest);state.catalog=catalog;state.locale=catalog.meta.defaultLocale;state.page=pageFromHash();if(!state.cart.fulfillmentModeId){const firstMode=catalog.order.fulfillmentModes.find((mode)=>mode.enabled);if(firstMode)state.cart.fulfillmentModeId=firstMode.id;}render();}).catch(()=>{document.documentElement.setAttribute("data-runtime-boot","error");app.innerHTML='<p class="catalog-error">'+escapeHtml(copy("Z"))+'</p>';});})();